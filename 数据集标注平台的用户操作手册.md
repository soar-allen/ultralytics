# 数据集标注平台用户操作手册

> 给日常使用人员看的简版说明。  
> 不需要懂技术，按步骤操作即可。

---

# 一、日常最常用流程

## 1. 新图片标注流程

1. 打开平台，先在左侧选择正确的数据集。
2. 点击左侧 `📥 导入与整理`。
3. 在 `📥 数据导入` 中导入新图片。
4. 点击左侧 `🤖 自动预标注`，先让系统自动标一遍。
5. 点击左侧 `🔄 CVAT 标注同步`，把图片推送到 CVAT。
6. 标注员在 CVAT 中检查、修改、补漏。
7. 回到平台，在 `🔄 CVAT 标注同步` 中把 CVAT 标注结果拉回来。
8. 点击 `📊 数据总览` 或 `🔬 质量检查`，检查标注是否正常。
9. 如果要训练模型，进入 `📤 导出与训练`。

## 2. 训练模型流程

1. 确认图片已经标注完成。
2. 点击左侧 `📤 导出与训练`。
3. 进入 `📤 数据导出`，导出训练数据。
4. 进入 `🏋️ 训练管理`，选择刚导出的数据。
5. 点击 `🚀 开始训练`。
6. 在 `训练进度与历史` 中查看训练结果。
7. 训练完成后，如需部署，再进入 `模型格式转换`。

---

# 二、打开平台后先做什么

## 1. 选择数据集

1. 看左侧菜单。
2. 在 `选择数据集` 下拉框中选择你要操作的数据集。
3. 看页面顶部“当前数据集”，确认没有选错。

注意：  
如果没有数据集，展开左侧 `数据集操作 → 新建数据集`，输入名称后点击 `创建`。

## 2. 打开图片查看器

1. 页面顶部有 `打开 FiftyOne` 按钮。
2. 点击后可以查看当前数据集里的图片和标注。
3. 如果只是想检查图片有没有导入成功，可以先打开这里看。

---

# 三、如何导入新图片

## 1. 最常用方式：导入一个图片文件夹

适合：新拍了一批图片，需要批量导入平台。

步骤：

1. 点击左侧 `📥 导入与整理`。
2. 进入上方 `📥 数据导入` 页签。
3. 在 `选择导入格式` 中选择 `A. 纯图片目录`。
4. 在 `批次标签` 中输入本批图片名字，例如：

```text
batch_01
```

5. 在 `图片目录路径` 中选择图片所在文件夹。
6. 如果图片在子文件夹里，勾选 `递归扫描子目录`。
7. 点击 `开始导入`。
8. 导入完成后，去 `📊 数据总览` 查看图片数量是否增加。

建议：

- 每次导入都写批次标签，例如 `batch_01`、`batch_02`。
- 后面筛选、标注、训练都会用到这个批次标签。

## 2. 从备份导入

适合：别人已经备份好一份数据，你要导入到当前数据集。

步骤：

1. 点击左侧 `📥 导入与整理`。
2. 进入 `📥 数据导入`。
3. 在 `选择导入格式` 中选择 `F. 从备份导入`。
4. 选择备份目录。
5. 如果页面显示备份信息，说明选对了。
6. 如果需要，填写 `额外标签`，例如：

```text
imported_backup
```

7. 点击 `📦 开始导入`。

## 3. 导入常见问题

### 图片数量不对怎么办

1. 检查图片是否真的在所选文件夹里。
2. 如果图片在子文件夹里，导入时勾选 `递归扫描子目录`。
3. 检查图片格式是否正常。
4. 还是不对时，联系管理员检查路径权限。

### 批次标签不知道怎么写

建议这样写：

1. 第一批：`batch_01`
2. 第二批：`batch_02`
3. 如果要加来源：`batch_02, factory_A`

---

# 四、如何自动标注

自动标注的作用：  
先让系统自动画一遍，人工再去 CVAT 检查和修改。

## 1. 使用 YOLO Pose 自动标托盘四角

步骤：

1. 点击左侧 `🤖 自动预标注`。
2. 选择 `YOLO Pose → 四角多边形`。
3. 选择模型。
   - 有现成模型时，选 `预设模型目录`。
   - 看不到模型时，选 `手动选择路径`。
4. `输出字段` 一般填 `ground_truth`。
5. `置信度阈值` 先保持默认。
6. 在 `预标注范围` 中选择：
   - 新图片：选 `仅无标注样本`。
   - 某一批图片：选 `按 Tags 筛选`，再选批次标签。
7. 点击 `🚀 开始 Pose 预标注`。
8. 完成后点击顶部 `打开 FiftyOne`，简单看一下是否有标注框。

注意：  
自动标注只是初稿，后面一定要人工检查。

## 2. 使用 SAM3 辅助标注

步骤：

1. 点击左侧 `🤖 自动预标注`。
2. 选择 `SAM3 辅助标注`。
3. 选择 SAM3 模型。
4. 选择输出格式：
   - 标托盘四边形：选 `四角多边形`。
   - 标普通目标框：选 `矩形框`。
5. `输出字段` 填 `ground_truth` 或 `predict`。
6. `输出类别名` 填目标名称，例如 `pallet`。
7. 在文本提示里输入目标名称，例如：

```text
pallet
wooden pallet
```

8. 选择预标注范围。
9. 点击 `🚀 开始 SAM3 预标注`。

---

# 五、如何把图片发到 CVAT 人工标注

## 1. 推送到 CVAT

步骤：

1. 点击左侧 `🔄 CVAT 标注同步`。
2. 进入 `推送到 CVAT`。
3. 在 `标注键 anno_key` 中输入任务名，例如：

```text
round1_batch01
```

4. `CVAT 项目名` 可以不填。
5. 字段来源选择 `选择已有字段`。
6. 标签字段一般选择 `ground_truth`。
7. 如果只推送某一批图片，在 `按批次标签筛选` 中选择 `batch_01`。
8. `每个 Job 图片数` 一般保持默认。
9. `图像质量` 一般保持 100。
10. 如需要标注员标记废弃图片，勾选 `启用废弃标记`。
11. 点击 `📤 推送到 CVAT`。
12. 推送成功后，通知标注员去 CVAT 处理。

注意：

- `anno_key` 不能重复。
- 建议命名清楚，例如 `round1_batch01`、`review_batch02`。

---

# 六、CVAT 中如何检查和修改标注

## 1. 登录 CVAT

1. 打开 CVAT 地址。
2. 输入账号和密码。
3. 登录后，确认右上角 Organization 是 `cotek`。
4. 点击顶部 `Jobs`。
5. 找到分配给自己的 Job。
6. 点击图片进入标注界面。

如果 Job 已经是 `Completed`，说明已经完成，不需要再标。

## 2. 标注界面怎么看

进入 Job 后，主要看三个地方：

1. 中间大图：当前图片。
2. 左侧工具栏：新增多边形或框。
3. 右侧面板：当前图片已有标注列表。

## 3. 检查框准不准

标准：

- 框要贴住托盘边缘。
- 不要切掉托盘角。
- 不要框进太多背景。

步骤：

1. 点击图片里的托盘框。
2. 鼠标放到角点上。
3. 按住左键拖动角点。
4. 调到贴合托盘边缘。
5. 按 `Ctrl + S` 保存。

## 4. 删除多余标注

多余标注包括：

- 把纸箱、木板、货架、阴影误标成托盘。
- 同一个托盘重复画了框。

步骤：

1. 点击错误的框。
2. 按键盘 `Delete` 或 `Backspace`。
3. 按 `Ctrl + S` 保存。

## 5. 补充漏标

漏标：图片里有托盘，但没有框。

步骤：

1. 点击左侧 `Draw new polygon`。
2. 选择正确类别：
   - `chuan`：川字托盘。
   - `tian`：田字托盘。
3. `Number of points` 输入 `4`。
4. 点击 `shape`。
5. 按顺序点托盘四个角：
   1. 左上
   2. 右上
   3. 右下
   4. 左下
6. 检查框是否贴边。
7. 按 `Ctrl + S` 保存。

提示：  
下一张图如果还要继续画同类框，可以按 `N` 快速开始。

## 6. 修改类别

步骤：

1. 点击要修改的框。
2. 在右侧面板找到这个标注。
3. 修改标签为正确类别。
4. 按 `Ctrl + S` 保存。

## 7. 标记遮挡点

什么时候需要：  
托盘能看清，但某个角被挡住了。

角点顺序：

1. 左上
2. 右上
3. 右下
4. 左下

步骤：

1. 选中托盘框。
2. 打开右侧详情，或右键进入 `DETAILS`。
3. 找到遮挡属性。
4. 勾选被遮挡的角点。
5. 按 `Ctrl + S` 保存。

## 8. 托盘怎么标才算正确

应该标：

1. 托盘主体清楚可见。
2. 托盘轻微遮挡，但还能判断边界。
3. 托盘在图片边缘，但可见部分足够多。
4. 一个托盘看到两个面，并且侧面比较明显时，两个面都要标。

可以不标：

1. 托盘严重遮挡，看不清结构。
2. 只露出很小一部分。
3. 看不到托盘入叉面的主要特征。

注意：

- 两个面都标时，两个框之间不要留缝。
- 看不清时先放大，不确定就截图发群里问。

## 9. CVAT 常用快捷键

- `F`：下一张。
- `D`：上一张。
- `Ctrl + S`：保存。
- `Delete` / `Backspace`：删除选中的框。
- `N`：开始画新框或新多边形。
- 鼠标滚轮：放大或缩小。
- 按住鼠标左键拖动：移动画面。
- 双击图片：恢复默认大小。

## 10. 完成 Job 后提交

1. 确认所有图片都检查完。
2. 按 `Ctrl + S` 保存。
3. 点击左上角 `Menu`。
4. 找到 `Change job state`。
5. 把状态改成 `Completed`。
6. 通知负责人。

---

# 七、如何把 CVAT 标注结果拉回平台

步骤：

1. 回到数据集标注平台。
2. 点击左侧 `🔄 CVAT 标注同步`。
3. 进入 `从 CVAT 拉取`。
4. 选择刚才的标注任务，也就是推送时填写的 `anno_key`。
5. 如果还想继续在 CVAT 修改，不要勾选“拉取后删除 CVAT 端任务”。
6. 点击 `📥 拉取标注`。
7. 拉取完成后，点击 `📊 数据总览` 查看类别数量。
8. 点击顶部 `打开 FiftyOne` 查看标注结果。

---

# 八、如何检查标注是否正确

## 1. 在平台中查看标注

步骤：

1. 点击左侧 `📊 数据总览`。
2. 查看样本总数和类别统计。
3. 进入 `标签筛选与查看`。
4. 选择批次标签，例如 `batch_01`。
5. 点击 `在 FiftyOne App 中展示筛选结果`。
6. 点击顶部 `打开 FiftyOne`。
7. 在 FiftyOne 中查看图片和标注。

## 2. 使用质量检查

步骤：

1. 点击左侧 `🔬 质量检查`。
2. 标签字段一般选择 `ground_truth`。
3. 在 `类别平衡` 中看各类别数量是否差太多。
4. 在 `面积分析` 中找特别小或特别大的框。
5. 在 `空标注检测` 中查有没有空标注。
6. 发现问题后，回到 CVAT 修改，再重新拉取。

## 3. 检查时重点看什么

1. 有没有漏标。
2. 有没有把非托盘误标成托盘。
3. 类别是否正确。
4. 四个角点是否贴边。
5. 遮挡点有没有勾选。
6. 两个面之间有没有留缝。

---

# 九、如何训练模型

## 1. 先导出训练数据

步骤：

1. 点击左侧 `📤 导出与训练`。
2. 进入 `📤 数据导出`。
3. 选择导出格式：
   - 托盘四角：选 `YOLO Pose (四边形转关键点)`。
   - 普通框：选 `YOLO Detect (纯框)`。
   - 旋转框：选 `YOLO OBB (旋转框)`。
4. 选择输出目录。
5. 如果只训练某一批，在 `按 Tag 筛选样本` 中选择批次。
6. 如果训练全部图片，Tags 留空。
7. 数据划分一般保持 `Train 80% / Valid 10% / Test 10%`。
8. 标签字段一般选择 `ground_truth`。
9. 如果只训练部分类别，在 `选择导出类别` 中选择。
10. 不确定类别时，留空表示导出全部类别。
11. 点击 `📦 开始导出`。
12. 记住页面显示的 `data.yaml` 路径。

## 2. 开始训练

步骤：

1. 进入 `🏋️ 训练管理`。
2. 打开 `训练配置与启动`。
3. 选择基础模型。
4. 选择任务类型：
   - 普通框：`detect`
   - 托盘四角：`pose`
   - 旋转框：`obb`
5. 选择刚导出的 `data.yaml`。
6. 运行名称可以不填，也可以填一个好记的名字。
7. 训练参数不熟悉时保持默认。
8. 点击 `🚀 开始训练`。
9. 进入 `训练进度与历史` 查看进度。

## 3. 查看训练结果

1. 在 `训练进度与历史` 查看训练是否完成。
2. 查看历史记录中的 `best.pt`。
3. 如果要用这个模型继续自动标注，点击 `设为当前最优权重` 或 `设为预标注模型`。

## 4. 训练效果不好怎么办

按顺序检查：

1. 标注是否准确。
2. 有没有漏标。
3. 类别有没有选错。
4. 训练图片是否太少。
5. 某些类别数量是否太少。
6. 光照、角度、场景是否太单一。
7. 用 `🔬 质量检查` 找异常。
8. 用 `🧠 高级工具 → 难例挖掘` 找模型容易错的图片。
9. 把难例重新推到 CVAT 修改，再重新训练。

---

# 十、如何导出或转换模型

## 1. 常用模型格式

- `onnx`：最常用，通用部署格式。
- `engine`：NVIDIA GPU 加速常用。
- `openvino`：Intel 平台常用。
- `tflite`：移动端或轻量设备。
- `torchscript`：PyTorch 使用。
- `ncnn` / `mnn` / `rknn`：端侧设备常用。

不确定时：

1. 先选 `onnx`。
2. 如果是 NVIDIA GPU 部署，再考虑 `engine`。

## 2. 转换模型格式

步骤：

1. 点击左侧 `📤 导出与训练`。
2. 进入 `🏋️ 训练管理`。
3. 打开 `🔁 模型格式转换`。
4. 在 `权重来源` 中选择：
   - `最新 best.pt`
   - 或 `历史权重`
   - 或 `自定义路径`
5. 在 `目标格式` 中选择格式，例如 `onnx`。
6. `任务类型` 不确定时选择自动检测。
7. `Image Size` 一般填 `640`。
8. `Device` 一般填 `0`，没有 GPU 可填 `cpu`。
9. 高级选项不熟悉时不要勾选。
10. 点击 `🔁 开始转换`。
11. 转换完成后，查看页面显示的输出路径。

## 3. 转换失败怎么办

1. 确认模型文件存在。
2. 先尝试转换 `onnx`。
3. 如果 `engine` 失败，确认机器是否支持 NVIDIA GPU。
4. 取消高级选项再试。
5. 仍失败时，把错误截图发给管理员。

---

# 十一、常见问题

## 1. CVAT 图片上传失败怎么办

1. 回到平台左侧 `⚙️ CVAT 配置`。
2. 点击 `🔗 测试连接`。
3. 检查 CVAT 地址、账号、密码、Organization。
4. 确认当前数据集里有图片。
5. 确认推送范围内不是 0 张图片。
6. 换一个新的 `anno_key` 再试。
7. 仍失败时，把错误截图发给管理员。

## 2. CVAT 找不到自己的任务怎么办

1. 确认右上角 Organization 是 `cotek`。
2. 点击顶部 `Jobs`。
3. 看 Assignee 是否是自己的账号。
4. 如果没有任务，联系负责人确认是否已分配。

## 3. CVAT 修改后平台里没变化怎么办

1. 在 CVAT 中按 `Ctrl + S` 保存。
2. 回到平台。
3. 点击 `🔄 CVAT 标注同步`。
4. 进入 `从 CVAT 拉取`。
5. 选择对应 `anno_key`。
6. 点击 `📥 拉取标注`。

## 4. 自动标注效果不好怎么办

1. 先检查是不是选错模型。
2. 调整置信度后再试。
3. 只选择需要处理的批次 Tags。
4. 自动标注后必须人工检查。
5. 错得多的图片可以作为难例重新标注。

## 5. 不小心导入错图片怎么办

1. 如果导入时写了批次标签，先到 `📊 数据总览` 按这个标签筛选。
2. 确认确实导错后，进入 `📥 导入与整理 → 🧹 处理清洗`。
3. 使用 `按标签删除样本` 删除该批图片。
4. 删除前确认是否要勾选 `同时删除磁盘文件`。

## 6. 不小心删错数据怎么办

1. 立即停止继续操作。
2. 联系管理员。
3. 如果之前做过备份，可以从 `🧠 高级工具 → 数据集备份` 恢复。

---

# 十二、最重要的提醒

1. 每次操作前，先确认顶部“当前数据集”。
2. 每批图片都写批次标签。
3. 自动标注后一定要人工检查。
4. CVAT 中每做几张图就按 `Ctrl + S` 保存。
5. 不确定是否要标，截图发群里问。
6. 删除数据前先确认，批量删除前最好先备份。

