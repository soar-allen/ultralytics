# 数据集标注平台功能概览

本文档简要说明当前数据集标注平台各部分的主要功能。平台入口为 `tools/dataset_platform/ui.py`，整体基于 Streamlit、FiftyOne、CVAT 和 Ultralytics YOLO 构建。

## 1. 侧边栏与工作区

- 数据集切换：选择当前 FiftyOne 数据集，并同步更新 FiftyOne App。
- 数据集操作：支持新建、重命名、删除数据集。
- 页面导航：进入数据总览、导入整理、自动预标注、CVAT 同步、导出训练、质量检查和高级工具等模块。
- CVAT 配置：设置 CVAT 地址、账号、密码和组织，并支持保存配置与连接测试。
- 工作区摘要：显示当前数据集、样本数、标签字段、Tags 数量，并提供 FiftyOne 快捷入口。

## 2. 数据总览

- 数据集统计：展示样本总数、标签字段数、Tags 种类数、标注实例总数。
- Tags 统计：统计不同样本标签的数量，便于查看批次、来源和状态分布。
- 标注类别统计：按标签字段统计各类别数量。
- 标签筛选与查看：按 Tags 筛选样本，并可在 FiftyOne 或 CVAT 中查看。
- 标签管理：批量添加/移除样本 Tags，支持类别统计、类别重命名等管理操作。
- 完整性检查：检查样本图片文件是否缺失，并支持清理缺失样本记录。

## 3. 导入与整理

### 数据导入

- 纯图片目录：导入未标注图片，可递归扫描子目录。
- Thoro COCO：导入 `coco_files/` 和 `image_groups/` 结构的数据。
- Roboflow COCO / YOLO：导入 Roboflow 标准导出格式，支持 train、valid、test split。
- CVAT 1.1 XML：从 CVAT XML 标注文件导入图片和标注。
- 备份导入：从平台完整备份中恢复图像、标注和 Tags 到当前数据集。

### 数据处理与清洗

- 无标注清理：查找并删除没有标注的样本，可选择是否删除磁盘文件。
- 异常图片检测：扫描损坏图片或尺寸异常图片，并支持删除。
- 按标签删除：按 Tags 批量删除样本。
- 重复图像清理：支持精确哈希去重、CLIP/DINOv2 近似去重，可打 `duplicate` 标签或直接删除。
- 多边形处理：支持多边形转四角、托盘多边形异常检测，并用 `bad_polygon` 标记问题样本。
- 字段管理：支持添加字段、删除字段、清空字段值、合并标签字段。

## 4. 自动预标注

- YOLO Pose 转四角多边形：使用关键点模型预测四个角点，并生成闭合四边形 Polyline。
- SAM3 辅助标注：支持文本提示、框示例、文本+示例联合提示，以及 SAM2 兼容视觉分割模式，输出 BBox 标注。
- SAM3 图片打标签：只判断图片是否包含指定目标，不生成标注，仅给样本添加 Tags。
- 范围控制：可对无标注样本、整个数据集或按 Tags 筛选后的样本执行预标注。
- 去重与失败处理：支持跳过已有标注、NMS 去重，并可为预测失败样本打标签。

## 5. CVAT 标注同步

- 推送到 CVAT：将数据集或筛选视图推送为 CVAT Task，可携带已有标注作为预标注。
- 多字段推送：可同时选择多个标签字段，并按字段生成独立标注任务。
- 标注类型支持：支持矩形框、多边形、关键点和分类标注。
- 拉取标注：按 `anno_key` 从 CVAT 同步标注结果回 FiftyOne。
- 运行管理：查看和删除标注运行记录，可选择是否同步清理 CVAT 端任务。
- 任务状态看板：查询 CVAT Job 状态、阶段、标注员和完成进度，并提供 Task 快捷链接。
- 废弃图片管理：标注员可在 CVAT 标记需废弃图片，平台支持打标签、移除样本或删除文件。

## 6. 导出与训练

### 数据导出

- YOLO Detect：导出检测框训练格式。
- YOLO Pose：导出关键点训练格式。
- YOLO 混合训练：将四边形目标转关键点，同时将指定检测类写成 bbox + 全 0 关键点。
- YOLO Pose 四边形转关键点：将 4 点多边形转为 tl/tr/br/bl 关键点。
- YOLO OBB：导出旋转框格式。
- 纯图片导出：仅导出图片文件。
- 导出控制：支持 Tags 筛选、类别筛选、train/valid/test 划分、background 负样本加入 train、类别顺序锁定。

### 训练管理

- 训练配置：选择模型、任务类型、data.yaml、输出目录和训练超参数。
- 后台训练：调用 Ultralytics 训练流程，训练时不阻塞页面。
- 增量训练：支持新增类别训练、旧类回放、类别顺序锁定和少数类重采样。
- 训练进度：显示当前 epoch、耗时、预计剩余时间、训练曲线和关键指标。
- 训练历史：记录历史训练参数、指标、best.pt 路径等信息。
- 权重管理：可将 best.pt 设置为后续预标注模型。
- 模型转换：支持将训练模型导出为 ONNX、TensorRT、OpenVINO 等格式。
- 通用预测与回灌：支持用训练后的模型预测数据，并将结果用于后续评估和难例挖掘。

## 7. 质量检查

- 类别平衡分析：统计各类别数量，提示过采样、欠采样或加权训练建议。
- 过采样工具：复制少数类样本，可带随机翻转、旋转、色彩增强，并同步变换多边形标注。
- 面积分析：检测过小或过大的标注框，并可在 FiftyOne 中查看异常样本。
- 空标注检测：查找有标签字段但内容为空的样本。
- 标注一致性：对比不同 `anno_key` 的类别分布，辅助发现标注员偏差。

## 8. 高级工具

- 难例挖掘：对比真值字段和预测字段，找出高错误率样本。
- 难例重标注：将难例打上 `hard_sample` 标签，并推送到 CVAT 重新标注。
- FiftyOne Brain：支持 uniqueness、hardness、representativeness 分析，用于去重、主动学习和数据覆盖度评估。
- 数据集备份：完整备份图片、标注和元数据，支持查看、恢复和删除已有备份。

## 9. 推荐闭环流程

```text
数据导入 -> 自动预标注 -> 推送 CVAT -> 人工标注 -> 拉取标注
-> 质量检查 -> 数据导出 -> 训练 -> 训练后回灌
-> 难例挖掘 -> 推送 CVAT 重标注 -> 继续迭代
```
