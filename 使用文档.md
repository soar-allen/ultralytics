# CV 数据集管理与标注平台使用文档

> 飞书复制版：按新版平台结构整理。  
> 适合直接复制到飞书文档，使用飞书左侧大纲快速跳转。

---

# 一、平台总览

## 1. 启动方式

在 `ultralytics` 项目根目录执行：

```bash
pip install -r tools/dataset_platform/requirements.txt
streamlit run tools/dataset_platform/ui.py
```

启动后进入 Streamlit 页面。

页面布局：

| 区域 | 作用 |
| --- | --- |
| 左侧侧边栏 | 选择数据集、切换工作流页面、数据集操作、CVAT 配置 |
| 顶部工作区摘要 | 显示当前数据集、样本数、标签字段数、Tags 数量、FiftyOne 快捷入口 |
| 主工作区 | 当前页面的具体功能 |

## 2. 新版页面结构

新版平台将功能按工作流重新分组，当前一级导航如下：

| 一级页面 | 包含功能 | 适合做什么 |
| --- | --- | --- |
| `📊 数据总览` | 统计、Tags 筛选、标签管理、完整性检查 | 看数据状态、管理 Tags 和类别 |
| `📥 导入与整理` | 数据导入、处理清洗 | 数据入库、删除异常/重复、字段管理 |
| `🤖 自动预标注` | YOLO Pose、SAM3 辅助标注、SAM3 标签识别 | 自动生成标注或给图片打标签 |
| `🔄 CVAT 标注同步` | 推送、拉取、运行管理、任务状态、废弃图片管理 | 与 CVAT 人工标注闭环 |
| `📤 导出与训练` | 数据导出、训练管理、模型转换、预测、回灌 | 导出训练集、训练模型、模型推理 |
| `🔬 质量检查` | 类别平衡、过采样、面积分析、空标注、一致性 | 检查标注质量 |
| `🧠 高级工具` | 难例挖掘、FiftyOne Brain、完整备份 | 闭环优化、分析、备份恢复 |

## 3. 常用概念

| 概念 | 含义 | 示例 |
| --- | --- | --- |
| Dataset | FiftyOne 数据集，平台所有操作的对象 | `pallet_v1` |
| Sample | 数据集中的单张图片 | 一张 `.jpg` |
| Tags | 样本标签，用来标记批次、来源、状态 | `batch_01`, `duplicate`, `reviewed` |
| Label Field | 标注字段，用来保存框、多边形、关键点、分类结果 | `ground_truth`, `predictions` |
| anno_key | 一次 CVAT 标注运行的唯一标识 | `round1_batch01` |
| CVAT Task | CVAT 中的任务 | 一个批次的图片任务 |
| CVAT Job | Task 自动切分后的工作单元 | 每 200 张图一个 Job |
| data.yaml | YOLO 训练数据配置文件 | 导出后生成 |
| best.pt | 训练得到的最优模型权重 | 用于预标注、预测、转换 |

## 4. 常用目录

| 路径 | 用途 |
| --- | --- |
| `~/.dataset_platform/model/` | 自动预标注模型目录 |
| `~/.dataset_platform/base_model/` | 训练基础模型目录 |
| `~/.dataset_platform/config.yaml` | 平台本地配置文件 |
| `/home/cotek/datasets/temp/data.yaml` | 训练页面默认 data.yaml 路径 |
| `/home/cotek/ws_cotek/ultralytics/detection_images` | 通用预测默认输入目录 |

## 5. 推荐 Tags 命名

| Tag | 建议用途 |
| --- | --- |
| `batch_01`, `batch_02` | 数据批次 |
| `factory_A`, `line_3` | 数据来源 |
| `duplicate` | 重复图片 |
| `corrupt`, `abnormal_size` | 异常图片 |
| `bad_polygon` | 异常多边形 |
| `background` | 背景负样本 |
| `hard_sample` | 难例 |
| `discarded` | 废弃图片 |
| `oversampled` | 过采样生成样本 |
| `reviewed`, `approved` | 审核状态 |

---

# 二、左侧侧边栏

## 1. 选择数据集

用途：切换当前操作的数据集。

操作步骤：

1. 在左侧“选择数据集”下拉框中选择数据集。
2. 平台会切换当前数据集。
3. 如果 FiftyOne 已启动，会同步切换展示的数据集。

注意：

- 没有数据集时，需要先新建。
- 大多数功能必须先选择数据集。

## 2. 页面导航

用途：切换一级工作流页面。

当前页面：

- `📊 数据总览`
- `📥 导入与整理`
- `🤖 自动预标注`
- `🔄 CVAT 标注同步`
- `📤 导出与训练`
- `🔬 质量检查`
- `🧠 高级工具`

操作步骤：

1. 在“页面导航”单选列表中选择页面。
2. 主工作区自动切换到对应页面。

## 3. 数据集操作

### 3.1 新建数据集

需要填写：

- `数据集名称`

操作步骤：

1. 展开“➕ 新建数据集”。
2. 输入数据集名称。
3. 点击“创建”。

### 3.2 重命名数据集

需要填写：

- `新名称`

操作步骤：

1. 展开“✏️ 重命名数据集”。
2. 输入新名称。
3. 点击“确认重命名”。

### 3.3 删除数据集

需要填写：

- `输入数据集名称确认删除`

操作步骤：

1. 展开“🗑️ 删除数据集”。
2. 输入当前数据集完整名称。
3. 点击“确认删除”。

注意：

- 这是高风险操作。
- 删除的是 FiftyOne 数据集记录；是否删除图片文件取决于具体功能是否提供“同时删除磁盘文件”选项。

## 4. CVAT 配置

新版中 CVAT 配置默认收起在“⚙️ CVAT 配置”中。

需要填写：

| 字段 | 说明 |
| --- | --- |
| `CVAT URL` | CVAT 服务地址，如 `http://192.168.1.89:8080` |
| `用户名` | CVAT 用户名 |
| `密码` | CVAT 密码 |
| `Organization（可选）` | CVAT 组织 slug；个人空间可留空 |

操作步骤：

1. 展开“⚙️ CVAT 配置”。
2. 填写 URL、用户名、密码、Organization。
3. 点击“💾 保存配置”。
4. 点击“🔗 测试连接”。
5. 连接成功后会显示服务器版本、项目数、任务数、组织信息。

注意：

- 如果账号属于 CVAT 组织空间，Organization 通常需要填写。
- 配置保存到 `~/.dataset_platform/config.yaml`。

## 5. 侧边栏数据集摘要

侧边栏底部会显示：

- 样本数。
- 标签字段。
- 前若干个样本 Tags。

---

# 三、顶部工作区摘要

新版平台每个页面顶部都会显示“工作区摘要”。

显示内容：

| 内容 | 说明 |
| --- | --- |
| 当前数据集 | 当前选中的 Dataset 名称 |
| 样本数 | 当前数据集中图片数量 |
| 标签字段 | Label Field 数量 |
| Tags | 样本 Tag 数量 |
| 打开 FiftyOne | 快速打开本地 FiftyOne App |
| 标签字段预览 | 显示前几个标签字段 |
| 样本 Tags 预览 | 显示前几个 Tags |
| 最近导出 data.yaml | 如果刚导出过训练集，会显示路径 |

使用建议：

1. 每次操作前先看“当前数据集”，避免操作错数据集。
2. 导出训练集后，可在这里确认最近 `data.yaml` 是否已记录。
3. 需要查看图片时，点击“打开 FiftyOne”。

---

# 四、通用路径选择器

多个功能会用到路径选择器，例如图片目录、模型权重、输出目录、备份目录。

使用方式：

1. 可以直接手动输入绝对路径。
2. 可以点击“📂 浏览”打开系统文件选择器。
3. 如果系统选择器不可用，会出现 Web 文件浏览器。

Web 文件浏览器操作：

| 控件 | 作用 |
| --- | --- |
| 当前目录 | 手动输入或查看当前浏览目录 |
| `⬆️` | 返回上级目录 |
| 子目录下拉框 | 选择后自动进入子目录 |
| `✅ 选择当前目录` | 目录模式下确认当前目录 |
| 文件下拉框 | 文件模式下选择文件并确认 |

---

# 五、📊 数据总览

数据总览用于查看数据集状态、按 Tags 筛选、管理 Tags 和类别。

## 1. 页面页签

| 页签 | 功能 |
| --- | --- |
| `📊 数据集统计` | 样本数、Tags 分布、类别统计、完整性检查 |
| `🔎 标签筛选与查看` | 按 Tags 筛选，并在 FiftyOne / CVAT 查看 |
| `🏷️ 标签管理` | 批量添加/移除 Tags、类别重命名、批量删除标注 |

## 2. 数据集统计

用途：查看当前数据集整体情况。

显示内容：

| 指标 | 含义 |
| --- | --- |
| 样本总数 | 当前数据集图片数量 |
| 标签字段数 | 标注字段数量 |
| Tags 种类 | 样本 Tags 种类数 |
| 标注实例总数 | 所有标注字段中的标注数量 |
| 样本 Tags 统计 | 每个 Tag 对应多少样本 |
| 标注类别统计 | 每个标签字段中各类别数量 |

操作步骤：

1. 打开 `📊 数据总览`。
2. 进入 `📊 数据集统计`。
3. 查看顶部指标。
4. 查看 Tags 统计。
5. 展开标签字段，查看类别分布。

## 3. 修复重复 Tags

用途：清理同一样本中重复出现的 Tags。

操作步骤：

1. 在 `📊 数据集统计` 中展开“🔧 修复重复 Tags”。
2. 点击“🔍 扫描并修复”。

## 4. 数据集完整性检查

用途：检查样本记录指向的图片文件是否还存在。

操作步骤：

1. 在 `📊 数据集统计` 中展开“🔍 数据集完整性检查”。
2. 点击“运行检查”。
3. 查看“文件存在”和“文件缺失”数量。
4. 如果有缺失文件，可展开缺失文件列表。
5. 如需移除缺失样本记录，勾选确认后点击“🗑️ 移除缺失样本”。

注意：

- 该功能只移除数据集记录，不会恢复丢失图片。

## 5. 按 Tags 筛选样本

用途：按 Tags 过滤样本，并查看筛选结果。

需要选择：

- `选择 Tags`：可多选；多选为 OR 关系。

操作步骤：

1. 进入 `🔎 标签筛选与查看`。
2. 选择一个或多个 Tags。
3. 查看筛选结果数量。
4. 点击“👁️ 在 FiftyOne App 中展示筛选结果”。
5. 点击“🌐 打开 FiftyOne 查看”。
6. 如样本已推送过 CVAT，可点击“🔗 打开 CVAT 面板”。
7. 可展开“📊 筛选结果统计”查看筛选子集的类别分布。

## 6. 刷新 CVAT Job 状态标签

用途：从 CVAT 同步 Job 最新状态到样本 Tags。

需要选择：

- `选择标注运行`：一个或多个 anno_key。

操作步骤：

1. 进入 `🔎 标签筛选与查看`。
2. 找到“🔄 CVAT Job 状态标签刷新”。
3. 选择标注运行。
4. 点击“🔄 刷新 Job 状态标签”。
5. 刷新后可按状态 Tags 筛选样本。

## 7. 样本 Tags 管理

用途：给样本批量添加或移除 Tags。

需要选择：

| 字段 | 选项 |
| --- | --- |
| 操作范围 | 整个数据集 / 按已有标签筛选 / 按标注字段筛选 |
| 操作 | 添加标签 / 移除标签 |

添加标签时填写：

- `要添加的标签（逗号分隔）`，例如 `batch_02, reviewed`

移除标签时选择：

- `选择要移除的标签`

操作步骤：

1. 进入 `🏷️ 标签管理`。
2. 选择操作范围。
3. 如果按已有标签筛选，选择范围 Tags。
4. 如果按标注字段筛选，选择字段和“有标注/无标注”。
5. 选择添加或移除。
6. 输入或选择 Tags。
7. 点击添加/移除按钮。

## 8. 类别重命名

用途：把某个标注字段中的类别批量改名。

需要填写：

- `选择标签字段`
- `选择要重命名的类别`
- `新类别名称`

操作步骤：

1. 进入 `🏷️ 标签管理`。
2. 打开“✏️ 类别重命名”。
3. 选择标签字段。
4. 选择旧类别。
5. 输入新类别名称。
6. 点击“✏️ 执行重命名”。

## 9. 批量删除指定类别标注

用途：删除某字段中指定类别的所有标注实例，不删除样本。

需要选择：

- `选择标签字段`
- `选择要删除的类别`
- 勾选确认删除

操作步骤：

1. 进入 `🏷️ 标签管理`。
2. 打开“🗑️ 批量删除标注”。
3. 选择标签字段。
4. 选择要删除的类别。
5. 核对将删除的实例数量。
6. 勾选确认。
7. 点击“🗑️ 执行批量删除”。

注意：

- 不删除样本，只删除标注。
- 操作不可撤销。

---

# 六、📥 导入与整理

新版 `📥 导入与整理` 页面把“数据导入”和“处理清洗”合并在同一个工作流页面中。

## 1. 页面页签

| 页签 | 功能 |
| --- | --- |
| `📥 数据导入` | 导入图片、COCO、YOLO、CVAT XML、平台备份 |
| `🧹 处理清洗` | 清理无效/重复/异常数据，多边形处理，字段管理 |

---

## 2. 📥 数据导入

### 2.1 导入格式速查

| 格式 | 适用场景 | 关键输入 |
| --- | --- | --- |
| A. 纯图片目录 | 只有图片，没有标注 | 图片目录、是否递归 |
| B. Thoro COCO | Thoro COCO 数据结构 | 根目录、标签字段、JSON 映射 |
| C. Roboflow COCO | Roboflow COCO 导出 | 根目录、标签字段、Split |
| D. Roboflow YOLO | Roboflow YOLO 导出 | 含 `data.yaml` 的根目录、Split |
| E. CVAT 1.1 XML | CVAT XML 离线标注 | XML 路径、图片目录、标签字段 |
| F. 从备份导入 | 平台完整备份迁移 | 备份目录、额外 Tags |

### 2.2 通用字段：批次标签

除“从备份导入”外，其余导入格式都有：

- `批次标签（逗号分隔，用于区分不同批次数据）`

示例：

```text
batch_02, factory_A
```

建议：

- 每批导入都填写批次标签，后续方便筛选、推送 CVAT、删除、导出。

### 2.3 A. 导入纯图片目录

需要填写：

- `图片目录路径`
- `递归扫描子目录`

操作步骤：

1. 选择“A. 纯图片目录”。
2. 填写批次标签。
3. 选择图片目录。
4. 按需勾选递归扫描。
5. 点击“开始导入”。

### 2.4 B. 导入 Thoro COCO

目录要求：

- 根目录包含 `coco_files/` 和 `image_groups/`。

需要填写：

| 字段 | 说明 |
| --- | --- |
| 数据集根目录 | Thoro COCO 根目录 |
| 标签字段名 | 默认 `ground_truth` |
| 标签映射 JSON | 可选，如 `{"旧名": "新名"}` |
| 关键点名称 JSON | 可选，如 `{"类别名": ["kp0", "kp1"]}` |
| 同时导入 BBox | 即使有关键点/多边形，也保留 bbox |

操作步骤：

1. 选择“B. Thoro COCO 格式”。
2. 填写批次标签。
3. 选择根目录。
4. 填写标签字段名。
5. 按需填写 JSON 映射。
6. 按需勾选导入 BBox。
7. 点击“开始导入”。

### 2.5 C. 导入 Roboflow COCO

需要填写：

- `数据集根目录`
- `标签字段名`
- `选择 Split`：`train` / `valid` / `test`

操作步骤：

1. 选择“C. Roboflow COCO 格式”。
2. 填写批次标签。
3. 选择根目录。
4. 选择 Split。
5. 点击“开始导入”。

### 2.6 D. 导入 Roboflow YOLO

需要填写：

- `数据集根目录（含 data.yaml）`
- `标签字段名`
- `选择 Split`

操作步骤：

1. 选择“D. Roboflow YOLO 格式”。
2. 填写批次标签。
3. 选择包含 `data.yaml` 的目录。
4. 选择 Split。
5. 点击“开始导入”。

### 2.7 E. 导入 CVAT XML

需要填写：

- `annotations.xml 路径`
- `图片目录（可选，默认为 XML 同级 images/）`
- `标签字段名`

操作步骤：

1. 选择“E. CVAT 1.1 XML 格式”。
2. 填写批次标签。
3. 选择 XML 文件。
4. 如图片不在默认位置，选择图片目录。
5. 点击“开始导入”。

### 2.8 F. 从备份导入

需要填写：

- `备份目录路径`：必须包含 `backup_info.json`
- `额外标签（逗号分隔，可选）`

操作步骤：

1. 选择“F. 从备份导入”。
2. 选择备份目录。
3. 平台识别备份后会显示备份信息。
4. 按需填写额外标签。
5. 点击“📦 开始导入”。

注意：

- 原始 Tags 会保留。
- 已存在同名文件会跳过。

---

## 3. 🧹 处理清洗

### 3.1 功能速查

| 功能 | 用途 |
| --- | --- |
| 无标注图像 | 删除无标注样本 |
| 损坏/异常图像 | 扫描打不开或尺寸异常的图片 |
| 多边形转四角 | 将多点 polygon 转为四点并排序 |
| 托盘多边形检测 | 检测异常 polygon，并打 `bad_polygon` |
| 按标签删除样本 | 删除带指定 Tags 的样本 |
| 重复图像清理 | 精确哈希/近似嵌入去重 |
| 添加字段 | 创建空 Label Field |
| 删除字段 | 永久删除字段或清空字段值 |
| 字段合并 | 将一个字段合并到另一个字段 |

### 3.2 删除无标注图像

需要选择：

- `同时删除磁盘文件`
- `确认删除 ... 个无标注样本`

操作步骤：

1. 进入 `🧹 处理清洗`。
2. 打开 `🧹 图像清理`。
3. 查看无标注样本数。
4. 按需勾选“同时删除磁盘文件”。
5. 勾选确认。
6. 点击“🗑️ 删除无标注样本”。

### 3.3 扫描异常图像

需要填写：

- `最小尺寸 (px)`，默认 32。
- `最大尺寸 (px)`，默认 20000。

操作步骤：

1. 设置尺寸范围。
2. 点击“🔍 扫描异常图像”。
3. 如发现异常，勾选确认。
4. 点击“🗑️ 删除异常样本（含磁盘文件）”。

### 3.4 多边形转四角

需要选择：

- `多边形字段`
- `操作范围`：整个数据集 / 按标签筛选

操作步骤：

1. 在“多边形处理”中选择 Polylines 字段。
2. 选择操作范围。
3. 打开“🔄 多边形转四角”。
4. 点击“🔄 执行转换”。

结果：

- 多点 polygon 会转为四角。
- 已经四点的 polygon 会重新排序为 tl / tr / br / bl。
- 无法转换的保留原样。

### 3.5 托盘多边形检测

用途：检测不合格托盘面 polygon，并标记为 `bad_polygon`。

可检测：

- 非四角。
- 两点过近。
- 内角异常。
- 非凸。
- 面积过小。
- 极端细条形。

需要设置：

| 字段 | 说明 |
| --- | --- |
| 多边形字段 | Polylines 字段 |
| 操作范围 | 全部或按 Tags |
| 仅检测特定类别 | 可选 |
| 最小点对距离比 | 判断点是否过近 |
| 最小/最大内角 | 判断折叠或展平 |
| 最小面积 | 面积过小判异常 |
| 最小紧凑度 | 极端细条判异常 |

操作步骤：

1. 选择多边形字段。
2. 选择操作范围。
3. 打开“📐 托盘多边形检测”。
4. 按需选择特定类别。
5. 一般先使用默认高级参数。
6. 点击“🔍 扫描不合格托盘多边形”。

处理已标记异常样本：

- 删除：勾选确认后点击“🗑️ 删除已标记的异常样本”。
- 清标签：点击“🧹 清除 bad_polygon 标记”。

### 3.6 按标签删除样本

需要选择：

- `选择标签`
- `同时删除磁盘文件`
- 确认删除

操作步骤：

1. 选择要删除的 Tags。
2. 查看匹配样本数。
3. 按需勾选“同时删除磁盘文件”。
4. 勾选确认。
5. 点击“🗑️ 删除匹配样本”。

### 3.7 重复图像清理

需要选择：

| 字段 | 选项 |
| --- | --- |
| 扫描范围 | 整个数据集 / 按标签筛选 |
| 去重模式 | 精确哈希 / 近似嵌入 / 两者兼有 |
| 嵌入模型 | CLIP 或 DINOv2 |
| 余弦距离阈值 | 越小越严格，推荐 `0.01 ~ 0.05` |

操作步骤：

1. 选择扫描范围。
2. 选择去重模式。
3. 如选择近似嵌入，设置模型和阈值。
4. 点击“🔍 扫描重复图像”。
5. 查看可删除重复数量。
6. 选择“仅打标签不删除”或直接删除。
7. 点击“执行去重”。

已有 `duplicate` 样本：

- 删除：点击“🗑️ 删除已标记的重复样本”。
- 清标签：点击“🧹 清除 duplicate 标记”。

### 3.8 添加字段

需要填写：

- `新字段名称`
- `字段类型`：Detections / Polylines / Keypoints / Classifications
- `操作范围`

操作步骤：

1. 打开 `🗂️ 字段管理`。
2. 进入“➕ 添加字段”。
3. 输入字段名。
4. 选择字段类型。
5. 选择范围。
6. 点击“➕ 添加字段”。

注意：

- 字段名必须是合法标识符：字母或下划线开头，只包含字母、数字、下划线。

### 3.9 删除字段 / 清空字段值

删除模式：

| 模式 | 作用 |
| --- | --- |
| 从整个数据集永久删除字段 | 删除字段及其所有数据 |
| 仅清空筛选范围内的字段值 | 保留字段，只清空部分样本字段值 |

操作步骤：

1. 选择字段。
2. 选择删除模式。
3. 如清空字段值，选择范围并勾选确认。
4. 如永久删除字段，输入字段名确认。
5. 点击对应删除/清空按钮。

### 3.10 字段合并

用途：将一个标签字段合并到另一个字段。

需要选择：

- `源字段`
- `目标字段`
- `合并后删除源字段`

操作步骤：

1. 打开“🔀 字段合并”。
2. 选择源字段和目标字段。
3. 确认两者类型一致。
4. 按需勾选合并后删除源字段。
5. 点击“🔀 执行合并”。

---

# 七、🤖 自动预标注

## 1. 模式速查

| 模式 | 用途 | 输出 |
| --- | --- | --- |
| YOLO Pose → 四角多边形 | 用关键点模型检测四角 | Polyline |
| SAM3 辅助标注 | 用文本/范例提示生成标注 | Polyline 或 BBox |
| SAM3 标注标签 | 只判断图片是否包含目标 | Tags |

## 2. 模型来源

| 来源 | 说明 |
| --- | --- |
| 预设模型目录 | 扫描 `~/.dataset_platform/model/` |
| 手动选择路径 | 手动选择模型文件 |

支持文件：

- `.pt`
- `.pth`
- `.onnx`
- `.engine`

## 3. 预标注范围

| 范围 | 含义 |
| --- | --- |
| 仅无标注样本 | 只处理无标注样本 |
| 整个数据集 | 处理全部样本 |
| 按 Tags 筛选 | 只处理带指定 Tags 的样本 |

## 4. YOLO Pose → 四角多边形

需要填写：

| 字段 | 说明 |
| --- | --- |
| YOLO Pose 权重 | 模型文件 |
| 输出字段 | 默认 `ground_truth` |
| 置信度阈值 | 默认 0.25 |
| 仅保留类别 | 可选，逗号分隔 |
| 完全跳过已有标注样本 | 防止重复标注 |
| 标注 NMS IoU 阈值 | 与已有同类标注重叠超过阈值时抑制 |
| 预标注范围 | 无标注 / 全部 / Tags |

操作步骤：

1. 选择“YOLO Pose → 四角多边形”。
2. 选择权重。
3. 设置输出字段。
4. 设置置信度。
5. 按需填写类别过滤。
6. 设置去重选项。
7. 选择范围。
8. 点击“🚀 开始 Pose 预标注”。

## 5. SAM3 辅助标注

输出格式：

- 四角多边形 (Polyline)
- 矩形框 (BBox)

提示策略：

| 策略 | 说明 |
| --- | --- |
| 联合提示 | 文本 + 范例，推荐 |
| 文本概念 | 只输入目标文字 |
| 图像范例 | 用已有标注 bbox 作为视觉范例 |
| 视觉分割 | 对每个已有标注逐一分割 |

需要填写：

| 字段 | 说明 |
| --- | --- |
| SAM3 权重 | 模型文件 |
| FP16 半精度 | 显存支持时可开启 |
| 输出格式 | Polyline 或 BBox |
| 输出字段 | 写入字段 |
| 输出类别名 | 默认 `pallet` |
| 文本提示 | 每行一个提示词 |
| 来源标签 | 限定范例来源样本 |
| 来源字段 | 包含 Polyline/Detection 的字段 |
| 来源类别 | 为空表示全部类别 |
| BBox 扩展比例 | 扩大范例上下文 |
| 失败标签 | 默认 `sam3_failed` |

操作步骤：

1. 选择“SAM3 辅助标注”。
2. 选择权重。
3. 选择输出格式和输出字段。
4. 填写输出类别名。
5. 选择提示策略。
6. 填写文本提示或选择范例来源。
7. 设置 BBox 扩展比例。
8. 设置去重和失败标签。
9. 选择预标注范围。
10. 点击“🚀 开始 SAM3 预标注”。

注意：

- 文本模式必须填写文本提示。
- 图像范例/视觉分割必须选择来源字段。
- 联合提示至少需要文本提示或来源字段之一。

## 6. SAM3 标注标签

用途：只识别图片是否包含目标，不生成标注，只打 Tags。

需要填写：

| 字段 | 说明 |
| --- | --- |
| SAM3 权重 | 模型文件 |
| 置信度阈值 | 判断目标存在的阈值 |
| 识别成功标签 | 默认 `sam3_found` |
| 未识别标签 | 可选 |
| 预测失败标签 | 默认 `sam3_tag_failed` |
| 提示策略 | 联合提示 / 文本 / 图像范例 |
| 识别范围 | 全部 / 按 Tags |

操作步骤：

1. 选择“SAM3 标注标签”。
2. 选择权重。
3. 设置成功、未识别、失败标签。
4. 选择提示策略。
5. 填写文本提示或选择范例来源。
6. 选择识别范围。
7. 点击“🚀 开始 SAM3 标签识别”。

## 7. 清除标签

操作步骤：

1. 展开“🗑️ 清除标签”。
2. 选择要清除的 Tag。
3. 点击清除按钮。

---

# 八、🔄 CVAT 标注同步

## 1. 功能速查

| 页签 | 用途 |
| --- | --- |
| 推送到 CVAT | 上传图片和预标注到 CVAT |
| 从 CVAT 拉取 | 同步人工标注结果回数据集 |
| 管理标注运行 | 查看/删除 annotation run |
| 任务状态与审核 | 查询 Job 状态、处理废弃图片 |

## 2. 推送到 CVAT

需要填写：

| 字段 | 说明 |
| --- | --- |
| anno_key | 标注运行唯一键，如 `round1_batch01` |
| CVAT 项目名 | 可选，留空使用数据集名 |
| 字段来源 | 选择已有字段 / 输入新字段名 |
| 标签字段 | 推送已有标注作为预标注 |
| 标注类型 | 新字段模式下选择 detections / polylines / keypoints / classifications |
| 推送范围 | 全部 / 仅无标注 / 仅有标注 |
| 批次标签筛选 | 可选，只推送指定 Tags |
| 每个 Job 图片数 | 默认 200 |
| 图像质量 | 默认 100 |
| 启用废弃标记 | 允许标注员标记废弃图片 |

操作步骤：

1. 打开“推送到 CVAT”。
2. 输入 anno_key。
3. 按需填写项目名。
4. 选择已有字段，或输入新字段名并选择标注类型。
5. 按需筛选类别。
6. 选择推送范围。
7. 按需选择批次 Tags。
8. 设置 Job 图片数和图像质量。
9. 按需启用废弃标记。
10. 点击“📤 推送到 CVAT”。

注意：

- anno_key 只能包含字母、数字、下划线、短横线。
- 每次推送必须使用不同 anno_key。
- 多字段推送会生成类似 `anno_key_字段名` 的派生 key。

## 3. CVAT 中标记废弃图片

如果推送时启用了废弃标记，标注员操作：

1. 打开 CVAT 标注任务。
2. 进入标注界面。
3. 切换到 `Tag` 模式。
4. 选择 `discard`。
5. 遇到废弃图片时点击画面添加 `discard`。
6. 切回 `Shape` 模式继续标注。

## 4. 从 CVAT 拉取

需要选择：

- `选择标注运行`：推送时使用的 anno_key。

可选项：

| 选项 | 说明 |
| --- | --- |
| 拉取后删除 CVAT 端任务 | 拉取完成后清理 CVAT 任务 |
| 跳过自动 Job 状态标签 | 大数据集可先跳过，后续在数据总览刷新 |

操作步骤：

1. 打开“从 CVAT 拉取”。
2. 选择标注运行。
3. 按需勾选清理 CVAT 端任务。
4. 按需勾选跳过自动状态标签。
5. 点击“📥 拉取标注”。

## 5. 管理标注运行

操作步骤：

1. 打开“管理标注运行”。
2. 查看运行表格。
3. 选择要删除的运行。
4. 按需勾选“同时清理 CVAT 端”。
5. 输入 anno_key 确认。
6. 点击“🗑️ 确认删除标注运行”。

## 6. 任务状态与审核

操作步骤：

1. 打开“任务状态与审核”。
2. 选择一个或多个标注运行。
3. 点击“🔍 查询 Job 状态”。
4. 查看 Job ID、Task ID、state、stage、标注员、帧范围、样本数。
5. 点击 Task 链接可跳转 CVAT。

## 7. 废弃图片管理

处理方式：

| 方式 | 说明 |
| --- | --- |
| 仅打标签 | 给废弃样本加 `discarded` 等 Tags |
| 从数据集移除（保留文件） | 删除样本记录，不删图片 |
| 从数据集移除并删除文件 | 删除样本记录和图片文件 |

操作步骤：

1. 在“任务状态与审核”中找到“废弃图片管理”。
2. 如有废弃样本，可先在 FiftyOne 中查看。
3. 选择处理方式。
4. 如果仅打标签，填写标签名。
5. 如果移除样本，输入数量确认。
6. 点击“✅ 执行处理”。

---

# 九、📤 导出与训练

新版 `📤 导出与训练` 页面把“数据导出”和“训练管理”合并在一个工作流页面中。

## 1. 页面页签

| 页签 | 功能 |
| --- | --- |
| `📤 数据导出` | 导出 YOLO Detect/Pose/OBB/纯图片 |
| `🏋️ 训练管理` | 训练配置、进度、模型转换、预测、回灌 |

---

## 2. 📤 数据导出

### 2.1 导出格式速查

| 格式 | 用途 |
| --- | --- |
| YOLO Detect (纯框) | 目标检测训练数据 |
| YOLO Pose (关键点) | bbox + keypoints |
| YOLO 混合训练（pose） | 四边形类转关键点，检测类写 bbox + 全 0 关键点 |
| YOLO Pose (四边形转关键点) | 4 点 polygon 转 tl/tr/br/bl |
| YOLO OBB (旋转框) | 旋转框四点 |
| 纯图片 | 只导出图片，不导出标签和 data.yaml |

### 2.2 通用导出设置

需要填写：

- `导出格式`
- `输出目录`
- `按 Tag 筛选样本（留空导出全部）`

操作步骤：

1. 选择导出格式。
2. 选择输出目录。
3. 按需选择 Tags。
4. 留空 Tags 表示导出全部。

### 2.3 数据划分

YOLO 格式需要设置。

| 模式 | 字段 |
| --- | --- |
| 按比例自动划分 | Train % / Valid % / Test %，总和必须 100 |
| 指定单个 Split | train / valid / test |

### 2.4 类别顺序锁定

用途：增量训练时保持旧类别 class id 不变。

操作步骤：

1. 展开“🔒 类别顺序锁定（增量训练推荐）”。
2. 选择上一版 `data.yaml`。
3. 确认平台读取旧类别。
4. 正常导出。

### 2.5 background 负样本

适用格式：

- YOLO 混合训练（pose）
- YOLO Pose (四边形转关键点)

用途：

- 将带 `background` Tag 的图片全部加入 train。
- 生成空 label 文件。

操作步骤：

1. 确保背景图带 `background` Tag。
2. 选择支持的导出格式。
3. 勾选“将 background 标签图片全部加入 train 作为负样本”。

### 2.6 各格式额外字段

| 格式 | 额外设置 |
| --- | --- |
| YOLO Detect | 标签字段、类别筛选 |
| YOLO Pose | 标签字段、关键点字段名、类别筛选 |
| YOLO 混合训练（pose） | 四边形字段、检测框字段、只检测类别、边界阈值、BBox 外扩 |
| YOLO Pose (四边形转关键点) | 四边形字段、边界阈值、BBox 外扩 |
| YOLO OBB | 标签字段、OBB 字段名 |
| 纯图片 | 无标签字段设置 |

### 2.7 开始导出

操作步骤：

1. 配置格式、输出目录、划分、字段、类别。
2. 点击“📦 开始导出”。
3. 查看导出结果。
4. 如果生成 `data.yaml`，平台会记录路径。
5. 顶部工作区摘要会显示最近导出的 `data.yaml`。
6. 可直接到训练管理使用。

---

## 3. 🏋️ 训练管理

### 3.1 页签速查

| 页签 | 用途 |
| --- | --- |
| 训练配置与启动 | 配模型、data.yaml、超参数并启动训练 |
| 训练进度与历史 | 看实时进度、曲线、历史、best.pt |
| 模型格式转换 | 导出 ONNX / TensorRT / OpenVINO 等 |
| 通用预测 | 对图片、视频、目录、URL 推理 |
| 训练后回灌 | 用 best.pt 预标注并评估 |

### 3.2 训练配置：模型

需要填写：

| 字段 | 说明 |
| --- | --- |
| 基础模型来源 | 基础模型目录 / 自定义路径 / 输入模型名称 |
| 基础模型目录 | `~/.dataset_platform/base_model/` |
| 任务类型 | detect / pose / obb / classify / segment |
| 训练输出目录 | 默认路径 / 自定义路径 |
| project | 默认 `/home/cotek/ws_cotek/ultralytics/runs/{task}` |

### 3.3 训练配置：数据

需要填写：

| 字段 | 说明 |
| --- | --- |
| data.yaml 来源 | 默认路径 / 自定义路径 |
| data.yaml 路径 | 最近导出路径或默认路径 |
| 运行名称 | 可留空自动命名 |

### 3.4 增量训练 / 旧类回放

用途：新增类别时保留旧类别能力。

需要填写：

- `启用增量训练回放`
- `新增类别`
- `每个旧类回放图片上限`
- `抽样种子`
- `新类自动重采样`

注意：

- 启用后必须选择新增类别。
- 推荐导出时先使用类别顺序锁定。

### 3.5 训练超参数

| 字段 | 说明 |
| --- | --- |
| Epochs | 训练轮数 |
| Image Size | 图片尺寸 |
| Batch Size | 批大小 |
| Device | `0` / `cpu` / `0,1` |

### 3.6 增强、损失和优化器

| 分组 | 字段 |
| --- | --- |
| 色彩空间增强 | hsv_h / hsv_s / hsv_v |
| 几何变换 | degrees / translate / scale / shear / perspective / fliplr / flipud |
| 组合增强 | mosaic / close_mosaic / mixup / cutmix / multi_scale |
| 类别平衡 | 少数类重采样、少数类名称、参考多数类、目标比例、最大重复倍数 |
| 损失权重 | cls / box / dfl |
| 优化器与学习率 | lr0 / lrf / weight_decay / optimizer / cos_lr / patience / momentum / warmup |

### 3.7 开始训练

操作步骤：

1. 确认 `data.yaml` 存在。
2. 确认模型路径或模型名称已填写。
3. 如启用增量训练，确认新增类别已选择。
4. 点击“🚀 开始训练”。
5. 在“训练进度与历史”查看进度。

### 3.8 训练进度与历史

训练中显示：

- 当前轮数。
- 已用时间。
- 预计剩余。
- 进度条。
- 当前指标。
- Loss 曲线。
- 精度曲线。

训练完成显示：

- 开始时间。
- 训练用时。
- Epochs。
- mAP、Precision、Recall、Loss。
- 完整训练记录。
- 历史训练表格。

### 3.9 最优权重管理

用途：管理当前数据集的 `last_best_pt`。

操作步骤：

1. 查看“当前最优权重”。
2. 点击“📋 设为预标注模型”，可用于自动预标注。
3. 可在历史权重中选择某个 best.pt。
4. 点击“设为当前最优权重”。

### 3.10 模型格式转换

权重来源：

- 最新 best.pt。
- 历史权重。
- 自定义路径。
- 输入模型名称。

转换参数：

| 字段 | 说明 |
| --- | --- |
| 目标格式 | ONNX / TensorRT / OpenVINO 等 |
| 任务类型 | 自动检测或手动选择 |
| Image Size | 如 `640` 或 `640 480` |
| Device | GPU 或 CPU |
| Batch Size | 导出 batch |
| ONNX Opset | 0 表示自动 |
| TensorRT Workspace GB | 0 表示自动 |
| INT8 校准数据比例 | INT8 校准使用 |
| 高级选项 | half / int8 / dynamic / simplify / nms / keras / optimize |

操作步骤：

1. 打开“模型格式转换”。
2. 选择权重来源。
3. 设置目标格式和参数。
4. 点击“🔁 开始转换”。
5. 查看输出路径和转换历史。

### 3.11 通用预测

输入源类型：

- 默认文件夹。
- 自定义文件夹。
- 图片/视频文件。
- URL / 流地址。

推理参数：

- 任务类型。
- 置信度。
- NMS IoU。
- 图片尺寸。
- Device。
- 最大检测数。
- FP16 半精度。

输出控制：

- 保存标注图/视频。
- 保存 txt 标签。
- txt 中包含置信度。
- 保存裁剪目标。
- 输出目录。
- 子目录名。
- 允许覆盖已有目录。

操作步骤：

1. 打开“通用预测”。
2. 选择模型权重。
3. 选择输入源并填写路径或 URL。
4. 设置推理参数。
5. 按需设置输出控制和显示控制。
6. 点击“🔮 开始预测”。

### 3.12 训练后回灌

用途：用当前 best.pt 对数据集预标注并评估，为难例挖掘准备。

需要填写：

| 字段 | 默认 |
| --- | --- |
| 预测结果字段 | `predictions` |
| 真值字段 | `ground_truth` |
| 任务类型 | detect / pose / obb |
| 置信度阈值 | 0.25 |
| 评估键 | `eval` |

操作步骤：

1. 确认已有当前最优权重。
2. 打开“训练后回灌”。
3. 填写预测字段和真值字段。
4. 选择任务类型。
5. 点击“🔄 执行回灌（预标注 + 评估）”。
6. 回灌后到“高级工具 → 难例挖掘”查看难例。

---

# 十、🔬 质量检查

## 1. 页签速查

| 页签 | 用途 |
| --- | --- |
| 类别平衡 | 查看类别数量是否均衡，并可过采样 |
| 面积分析 | 找异常小/大的 bbox |
| 空标注检测 | 找字段存在但内容为空的样本 |
| 标注一致性 | 对比多个 anno_key 的类别分布 |

## 2. 类别平衡

操作步骤：

1. 选择标签字段。
2. 打开“📊 类别平衡”。
3. 查看柱状图、表格、最大/最小类别比。
4. 查看平台给出的补采、过采样、欠采样建议。

## 3. 过采样工具

用途：复制少数类样本，并可随机增强。

需要填写：

- 目标数量策略：对齐到最大类 / 自定义目标数量。
- 每个类别目标实例数。
- 需要过采样的类别。
- 是否随机增强。
- 随机旋转角度范围。
- 是否只使用纯类别源图。
- 源图片允许包含的类别。

操作步骤：

1. 在“类别平衡”下方找到“过采样工具”。
2. 选择目标数量策略。
3. 选择过采样类别。
4. 查看计划表。
5. 按需开启随机增强和源样本限制。
6. 点击“🚀 执行过采样”。

结果：

- 新增样本带 `oversampled`。
- 新增样本带 `os_类别名`。

## 4. 面积分析

需要填写：

- `最小面积阈值 (%图像面积)`
- `最大面积阈值 (%图像面积)`

操作步骤：

1. 打开“📐 面积分析”。
2. 设置面积阈值。
3. 点击“🔍 分析面积分布”。
4. 查看异常小/异常大样本数。
5. 点击按钮在 FiftyOne 中查看异常样本。

## 5. 空标注检测

操作步骤：

1. 打开“🔲 空标注检测”。
2. 点击“🔍 检测空标注”。
3. 如有结果，点击“👁️ 在 FiftyOne 中查看”。

## 6. 标注一致性

用途：对比不同 CVAT 标注运行之间的类别分布。

操作步骤：

1. 打开“🔍 标注一致性”。
2. 选择至少 2 个标注运行。
3. 点击“🔍 分析一致性”。
4. 查看不同 anno_key 的类别数量对比。

---

# 十一、🧠 高级工具

## 1. 页签速查

| 页签 | 用途 |
| --- | --- |
| 难例挖掘 | 根据 predictions 和 ground_truth 找模型表现差的样本 |
| FiftyOne Brain | 唯一性、难度、代表性分析 |
| 数据集备份 | 完整备份、恢复、删除 |

## 2. 难例挖掘

需要填写：

| 字段 | 默认 |
| --- | --- |
| 预测字段 | `predictions` |
| 真值字段 | `ground_truth` |
| IoU 阈值 | 0.5 |
| 最多显示难例数 | 50 |

操作步骤：

1. 打开“🎯 难例挖掘”。
2. 填写预测字段和真值字段。
3. 设置 IoU 阈值。
4. 点击“📊 评估模型表现”。
5. 设置最多显示难例数。
6. 点击“🎯 挖掘难例”。
7. FiftyOne 中会展示难例视图。

## 3. 难例推送 CVAT 重标注

需要填写：

- 是否为难例打 `hard_sample` 标签。
- `CVAT 标注键`，默认 `hard_relabel`。
- `标注字段`。

操作步骤：

1. 先执行“挖掘难例”。
2. 勾选是否添加 `hard_sample`。
3. 填写 CVAT 标注键。
4. 填写标注字段。
5. 点击“📤 推送难例到 CVAT”。

## 4. FiftyOne Brain

| 功能 | 用途 | 输出字段 |
| --- | --- | --- |
| Uniqueness | 找相似/独特样本 | `uniqueness` |
| Hardness | 找模型最不确定样本 | `hardness` |
| Representativeness | 找代表性低的边缘样本 | `representativeness` |

Uniqueness 操作：

1. 点击“🔍 计算 Uniqueness”。
2. 查看最不唯一样本或最独特样本。
3. 点击“📊 查看统计”看均值、最小值、最大值。

Hardness 操作：

1. 填写预测字段。
2. 点击“🎯 计算 Hardness”。
3. 查看最难样本。

Representativeness 操作：

1. 点击“📊 计算 Representativeness”。
2. 查看代表性最低样本。

## 5. 数据集完整备份

用途：复制图像文件并导出元数据，创建可迁移完整备份。

创建备份需要填写：

- `备份根目录`
- `备份备注`
- `备份子目录名（可选）`

创建步骤：

1. 打开“💾 数据集备份”。
2. 选择备份根目录。
3. 填写备注。
4. 按需填写备份子目录名。
5. 点击“💾 创建完整备份”。

恢复步骤：

1. 选择已有备份。
2. 填写“恢复为数据集名称”。
3. 点击“♻️ 从备份恢复”。
4. 输入数据集名称确认。
5. 点击“✅ 确认恢复”。

删除备份步骤：

1. 选择已有备份。
2. 勾选“⚠️ 确认删除此备份”。
3. 点击“🗑️ 删除此备份”。

---

# 十二、推荐完整工作流

## 1. 从零开始做一批数据

1. 侧边栏新建数据集。
2. `导入与整理 → 数据导入`：导入图片或已有标注，填写批次 Tags。
3. `数据总览`：查看样本数、Tags、类别统计。
4. `自动预标注`：用 YOLO Pose 或 SAM3 生成初始标注。
5. `CVAT 标注同步`：推送到 CVAT。
6. CVAT 中人工检查、修正、补标。
7. `CVAT 标注同步`：拉取标注。
8. `质量检查`：查看类别平衡、面积异常、空标注、一致性。
9. `导入与整理 → 处理清洗`：删除异常、重复、无效样本。
10. `导出与训练 → 数据导出`：导出 YOLO 训练数据。
11. `导出与训练 → 训练管理`：启动训练。
12. 训练完成后设置 best.pt 为当前最优权重。
13. `训练后回灌`：生成 predictions 并评估。
14. `高级工具`：挖掘难例。
15. 推送难例到 CVAT 重标注，进入下一轮迭代。

## 2. 多批次数据管理

1. 每批导入都填写 Tags，例如 `batch_01`、`batch_02`。
2. 人工标注尽量统一写入 `ground_truth`。
3. 推送 CVAT 时按 Tags 筛选批次。
4. 每次推送使用独立 anno_key，例如 `round1_batch01`。
5. 拉取后在 `数据总览` 刷新 Job 状态标签。
6. 导出训练集时按 Tags 选择参与训练的样本。

## 3. 新增类别增量训练

1. 导入新类别数据并打批次 Tags。
2. 完成新类别标注。
3. `导出与训练 → 数据导出`：启用类别顺序锁定，选择上一版 `data.yaml`。
4. 确认旧类别顺序不变，新类别追加。
5. `训练管理`：启用“增量训练 / 旧类回放”。
6. 选择新增类别。
7. 设置旧类回放图片上限。
8. 启动训练。

---

# 十三、高风险操作提醒

执行以下操作前，建议先创建完整备份。

| 高风险操作 | 风险 |
| --- | --- |
| 删除数据集 | 删除当前 Dataset |
| 删除无标注样本并勾选删除磁盘文件 | 删除真实图片文件 |
| 删除异常样本 | 删除样本和图片 |
| 按标签删除样本 | 可能批量删错 |
| 删除重复样本 | 可能误删近似但不重复图片 |
| 永久删除字段 | 字段和标注全部丢失 |
| 批量删除类别标注 | 标注实例不可恢复 |
| 拉取后删除 CVAT 端任务 | CVAT 任务永久删除 |
| 删除标注运行并清理 CVAT | 本地记录和 CVAT 任务都删除 |
| 移除废弃图片并删除文件 | 删除样本和图片文件 |
| 从备份恢复覆盖数据集 | 同名数据集被覆盖 |
| 删除备份 | 备份目录被删除 |

建议：

1. 先打 Tags，再筛选查看，最后删除。
2. 删除前先在 FiftyOne 中查看样本。
3. 大批量操作前先完整备份。

---

# 十四、常见问题

## 1. 页面提示“请先在侧边栏选择或创建数据集”

处理：

1. 在左侧选择已有数据集。
2. 如果没有数据集，先新建数据集。

## 2. CVAT 推送失败

检查：

1. CVAT URL、用户名、密码是否正确。
2. Organization 是否需要填写。
3. 是否测试连接成功。
4. anno_key 是否已经存在。
5. 推送范围内是否有样本。
6. 标签字段、标注类型、类别是否正常。

## 3. 拉取后没有 CVAT 状态 Tags

可能原因：

- 拉取时勾选了“跳过自动 Job 状态标签”。
- 缺少 frame_id 映射。

处理：

1. 打开 `📊 数据总览`。
2. 进入 `🔎 标签筛选与查看`。
3. 在“CVAT Job 状态标签刷新”中选择 anno_key。
4. 点击刷新。

## 4. 训练页面找不到模型

处理：

1. 将模型放到 `~/.dataset_platform/base_model/`。
2. 或选择“自定义路径”手动选择。
3. 或选择“输入模型名称”，例如 `yolo11n.pt`。

## 5. 自动预标注找不到模型

处理：

1. 将模型放到 `~/.dataset_platform/model/`。
2. 或选择“手动选择路径”。
3. 确认扩展名是 `.pt`、`.pth`、`.onnx` 或 `.engine`。

## 6. 导出后训练页面没有自动填入 data.yaml

处理：

1. 确认导出格式不是“纯图片”。
2. 确认导出成功并生成 `data.yaml`。
3. 查看顶部工作区摘要是否显示最近导出的 `data.yaml`。
4. 在训练页面选择“自定义路径”，手动选择导出的 `data.yaml`。

## 7. 想撤销过采样

处理：

1. 过采样新增样本会带 `oversampled` 标签。
2. 在 `数据总览` 按 `oversampled` 筛选确认。
3. 在 `导入与整理 → 处理清洗` 中按标签删除样本。

