# CV 数据集管理与标注平台使用文档

> 适合直接复制到飞书文档。  
> 建议在飞书中使用左侧大纲快速跳转；每个页面都按“用途 / 需要填写 / 操作步骤 / 注意事项”整理。

---

# 目录

1. 平台启动与基础概念
2. 页面速查表
3. 侧边栏：数据集与 CVAT 配置
4. Data Hub：数据集管理中心
5. 数据导入
6. 处理清洗
7. 自动预标注
8. CVAT 同步
9. 数据导出
10. 训练管理
11. 质量检查
12. 高级功能
13. 推荐工作流
14. 高风险操作提醒
15. 常见问题

---

# 1. 平台启动与基础概念

## 1.1 启动方式

在 `ultralytics` 仓库根目录执行：

```bash
pip install -r tools/dataset_platform/requirements.txt
streamlit run tools/dataset_platform/ui.py
```

启动后进入 Streamlit 页面：

- 左侧：页面导航、数据集管理、CVAT 配置。
- 右侧：当前功能页面。

## 1.2 常用目录

| 目录 | 用途 |
| --- | --- |
| `~/.dataset_platform/model/` | 自动预标注模型目录，自动预标注页面会扫描这里的 `.pt` / `.pth` / `.onnx` / `.engine` |
| `~/.dataset_platform/base_model/` | 训练基础模型目录，训练页面会扫描这里的 `.pt` / `.pth` / `.yaml` |
| `~/.dataset_platform/config.yaml` | 平台本地配置文件，侧边栏保存 CVAT 配置后写入这里 |
| `/home/cotek/datasets/temp/data.yaml` | 训练页面默认 data.yaml 路径 |
| `/home/cotek/ws_cotek/ultralytics/detection_images` | 通用预测默认输入文件夹 |

## 1.3 基础概念

| 名称 | 含义 | 示例 |
| --- | --- | --- |
| Dataset | FiftyOne 数据集，平台操作的核心对象 | `pallet_v1` |
| Sample | 数据集中的一张图片样本 | 一张 `.jpg` |
| Tags | 样本标签，用于标记批次、来源、状态 | `batch_01`, `reviewed`, `duplicate` |
| Label Field | 标注字段，存放框、多边形、关键点、分类 | `ground_truth`, `predictions` |
| anno_key | CVAT 一次标注任务的唯一键 | `round1_all`, `hard_relabel` |
| CVAT Task | CVAT 中的标注任务 | 某一批图片任务 |
| CVAT Job | Task 自动切分后的工作单元 | 每 200 张图一个 Job |
| data.yaml | YOLO 训练数据配置文件 | 导出后生成 |
| best.pt | 训练得到的最优权重 | 用于预标注、预测、转换格式 |

## 1.4 推荐 Tags 命名

| Tag | 含义 |
| --- | --- |
| `batch_01`, `batch_02` | 数据批次 |
| `factory_A`, `line_3` | 数据来源 |
| `duplicate` | 重复图片 |
| `corrupt`, `abnormal_size` | 异常图片 |
| `bad_polygon` | 异常托盘多边形 |
| `hard_sample` | 难例 |
| `background` | 无目标背景图 |
| `train`, `valid`, `test` | 数据划分 |
| `reviewed`, `approved`, `discarded` | 审核状态 |
| `oversampled` | 过采样生成样本 |

---

# 2. 页面速查表

| 页面 | 主要用途 | 常用入口 |
| --- | --- | --- |
| `📊 Data Hub` | 看统计、筛选 Tags、管理 Tags、重命名/删除类别、刷新 CVAT 状态 | 数据集概览、批量打标签、按标签查看 |
| `📥 数据导入` | 导入纯图片、COCO、YOLO、CVAT XML、平台备份 | 新数据入库 |
| `🧹 处理清洗` | 删除无标注/异常/重复样本，多边形处理，字段管理 | 数据清洗、字段合并 |
| `🤖 自动预标注` | YOLO Pose 或 SAM3 自动生成标注，或只打 Tags | 预标注、自动筛选目标图 |
| `🔄 CVAT 同步` | 推送 CVAT、拉取标注、看 Job 状态、处理废弃图 | 人工标注闭环 |
| `📤 数据导出` | 导出 YOLO Detect/Pose/OBB/纯图片 | 训练前导出数据 |
| `🏋️ 训练管理` | 配置训练、看进度、管理 best.pt、模型转换、预测、回灌 | 训练与模型管理 |
| `🔬 质量检查` | 类别平衡、过采样、面积异常、空标注、一致性 | 标注质量分析 |
| `🧠 高级功能` | 难例挖掘、FiftyOne Brain、完整备份 | 迭代优化、备份恢复 |

---

# 3. 侧边栏：数据集与 CVAT 配置

## 3.1 页面导航

**用途**  
切换当前功能页面。

**操作步骤**

1. 在左侧“页面导航”中选择页面。
2. 页面会自动刷新到对应功能区。

## 3.2 选择数据集

**用途**  
切换当前正在操作的 FiftyOne 数据集。

**需要选择**

- `选择数据集`：已有数据集名称。

**操作步骤**

1. 在“选择数据集”下拉框选择目标数据集。
2. 平台自动切换当前数据集。
3. 如果 FiftyOne App 已启动，也会同步切换显示的数据集。

## 3.3 新建数据集

**用途**  
创建空数据集。

**需要填写**

- `数据集名称`

**操作步骤**

1. 展开“➕ 新建数据集”。
2. 输入数据集名称。
3. 点击“创建”。

## 3.4 重命名数据集

**用途**  
修改当前数据集名称。

**需要填写**

- `新名称`

**操作步骤**

1. 展开“✏️ 重命名数据集”。
2. 输入新名称。
3. 点击“确认重命名”。

## 3.5 删除数据集

**用途**  
删除当前数据集。

**需要填写**

- `输入数据集名称确认删除`

**操作步骤**

1. 展开“🗑️ 删除数据集”。
2. 输入当前数据集完整名称。
3. 点击“确认删除”。

**注意事项**

- 这是高风险操作。
- 删除数据集记录不一定删除图片文件；只有页面明确提供“同时删除磁盘文件”时才会删除图片文件。

## 3.6 CVAT 配置

**用途**  
配置 CVAT 连接，用于推送/拉取标注。

**需要填写**

| 字段 | 说明 |
| --- | --- |
| `CVAT URL` | CVAT 服务地址，例如 `http://192.168.1.89:8080` |
| `用户名` | CVAT 用户名 |
| `密码` | CVAT 密码 |
| `Organization（可选）` | CVAT 组织 slug；个人空间可留空 |

**操作步骤**

1. 填写 CVAT URL、用户名、密码、Organization。
2. 点击“💾 保存配置”。
3. 点击“🔗 测试 CVAT 连接”。
4. 连接成功后会显示服务器版本、项目数、任务数、组织信息。

**注意事项**

- 如果账号在 CVAT 组织空间下，Organization 通常需要填写。
- 配置优先级：环境变量 > 本地配置文件 > 代码默认值。

## 3.7 路径浏览器通用用法

平台很多地方会选择目录或文件，例如图片目录、模型权重、输出目录。

**操作方式**

1. 可以直接手动输入绝对路径。
2. 可以点击“📂 浏览”打开系统文件选择器。
3. 如果系统选择器不可用，会打开 Web 文件浏览器：
   - `当前目录`：手动输入目录。
   - `⬆️`：返回上级目录。
   - 目录模式：点击“✅ 选择当前目录”。
   - 文件模式：选择文件后自动填入路径。

---

# 4. Data Hub：数据集管理中心

入口：`📊 Data Hub`

## 4.1 数据集统计

**用途**  
查看当前数据集总体情况。

**能看到什么**

| 指标 | 含义 |
| --- | --- |
| 样本总数 | 图片数量 |
| 标签字段数 | 标注字段数量 |
| Tags 种类 | 样本标签种类 |
| 标注实例总数 | 所有字段里的标注实例总量 |
| 样本 Tags 统计 | 每个 Tag 对应多少样本 |
| 标注类别统计 | 每个字段中各类别数量 |

**操作步骤**

1. 打开“📊 数据集统计”。
2. 查看顶部指标。
3. 查看 Tags 分布。
4. 展开标注字段，查看各类别数量。

## 4.2 修复重复 Tags

**用途**  
清理同一样本中重复出现的 Tags。

**操作步骤**

1. 在“数据集统计”中展开“🔧 修复重复 Tags”。
2. 点击“🔍 扫描并修复”。

## 4.3 数据集完整性检查

**用途**  
检查数据集中的图片路径是否还真实存在。

**操作步骤**

1. 展开“🔍 数据集完整性检查”。
2. 点击“运行检查”。
3. 查看文件存在数和缺失数。
4. 如果有缺失文件，可展开列表查看。
5. 如需移除缺失样本记录，勾选确认后点击“🗑️ 移除缺失样本”。

**注意事项**

- 该功能只移除数据集记录，不会恢复丢失图片。

## 4.4 标签筛选与查看

**用途**  
按 Tags 筛选样本，并在 FiftyOne 或 CVAT 中查看。

**需要选择**

- `选择 Tags`：可多选；多选是 OR 关系，匹配任意一个 Tag 即保留。

**操作步骤**

1. 打开“🔎 标签筛选与查看”。
2. 选择一个或多个 Tags。
3. 查看筛选结果数量。
4. 点击“👁️ 在 FiftyOne App 中展示筛选结果”。
5. 如需打开 FiftyOne，点击“🌐 打开 FiftyOne 查看”。
6. 如需查看 CVAT 任务，点击“🔗 打开 CVAT 面板”。
7. 可展开“📊 筛选结果统计”查看筛选子集的类别分布。

## 4.5 刷新 CVAT Job 状态标签

**用途**  
从 CVAT 查询最新 Job 状态，并写入样本 Tags，方便按状态筛选。

**需要选择**

- `选择标注运行`：一个或多个 anno_key。

**操作步骤**

1. 在“标签筛选与查看”页签找到“CVAT Job 状态标签刷新”。
2. 选择标注运行。
3. 点击“🔄 刷新 Job 状态标签”。
4. 刷新后会显示各状态标签对应样本数。

## 4.6 样本标签管理

**用途**  
批量添加或移除样本 Tags。

**需要选择**

| 字段 | 选项 |
| --- | --- |
| 操作范围 | 整个数据集 / 按已有标签筛选 / 按标注字段筛选 |
| 操作 | 添加标签 / 移除标签 |

**添加标签需要填写**

- `要添加的标签（逗号分隔）`，例如 `batch_02, reviewed`

**移除标签需要选择**

- `选择要移除的标签`

**操作步骤**

1. 打开“🏷️ 标签管理”。
2. 选择操作范围。
3. 如果按已有标签筛选，选择要筛选的 Tags。
4. 如果按标注字段筛选，选择字段，并选择“有标注的样本”或“无标注的样本”。
5. 选择添加或移除标签。
6. 输入或选择标签。
7. 点击添加/移除按钮。

## 4.7 类别重命名

**用途**  
将某个标注字段中的类别批量改名。

**需要填写/选择**

- `选择标签字段`
- `选择要重命名的类别`
- `新类别名称`

**操作步骤**

1. 在“标签管理”中进入“✏️ 类别重命名”。
2. 选择标签字段。
3. 选择旧类别。
4. 输入新类别名称。
5. 点击“✏️ 执行重命名”。

## 4.8 批量删除标注

**用途**  
删除某些类别的所有标注实例，不删除图片样本。

**需要选择**

- `选择标签字段`
- `选择要删除的类别`
- 勾选确认框

**操作步骤**

1. 进入“🗑️ 批量删除标注”。
2. 选择标签字段。
3. 选择要删除的类别。
4. 核对将删除的实例数。
5. 勾选确认。
6. 点击“🗑️ 执行批量删除”。

**注意事项**

- 不可撤销。
- 只删除标注实例，不删除样本。

---

# 5. 数据导入

入口：`📥 数据导入`

## 5.1 导入格式速查

| 格式 | 适用场景 | 关键输入 |
| --- | --- | --- |
| A. 纯图片目录 | 只有图片，没有标注 | 图片目录、是否递归 |
| B. Thoro COCO | Thoro COCO 数据结构 | 根目录、标签字段、标签映射、关键点名称 |
| C. Roboflow COCO | Roboflow COCO 导出 | 根目录、标签字段、Split |
| D. Roboflow YOLO | Roboflow YOLO 导出 | 含 `data.yaml` 的根目录、标签字段、Split |
| E. CVAT 1.1 XML | CVAT XML 离线导入 | XML 路径、图片目录、标签字段 |
| F. 从备份导入 | 从平台完整备份恢复/迁移 | 备份目录、额外 Tags |

## 5.2 通用字段：批次标签

除“从备份导入”外，其他导入格式都有：

- `批次标签（逗号分隔，用于区分不同批次数据）`

示例：

```text
batch_02, factory_A
```

**建议**  
每次导入都填写批次标签，后续可以按批次筛选、推送 CVAT、删除、导出。

## 5.3 A. 导入纯图片目录

**用途**  
导入未标注图片。

**需要填写**

- `图片目录路径`
- `递归扫描子目录`：默认开启。

**操作步骤**

1. 选择“A. 纯图片目录”。
2. 填写批次标签。
3. 选择图片目录。
4. 按需勾选递归扫描。
5. 点击“开始导入”。

## 5.4 B. 导入 Thoro COCO

**用途**  
导入包含 `coco_files/` 和 `image_groups/` 的 Thoro COCO 数据。

**需要填写**

| 字段 | 说明 |
| --- | --- |
| 数据集根目录 | 必须包含 `coco_files/` 和 `image_groups/` |
| 标签字段名 | 默认 `ground_truth` |
| 标签映射 JSON | 可选，例如 `{"旧名": "新名"}` |
| 关键点名称 JSON | 可选，例如 `{"类别名": ["kp0", "kp1"]}` |
| 同时导入 BBox | 即使有关键点/多边形，也保留 bbox |

**操作步骤**

1. 选择“B. Thoro COCO 格式”。
2. 填写批次标签。
3. 选择数据集根目录。
4. 填写标签字段名。
5. 按需填写标签映射 JSON、关键点名称 JSON。
6. 按需勾选“同时导入 BBox”。
7. 点击“开始导入”。

## 5.5 C. 导入 Roboflow COCO

**需要填写/选择**

- `数据集根目录`
- `标签字段名`：默认 `ground_truth`
- `选择 Split`：`train` / `valid` / `test`

**操作步骤**

1. 选择“C. Roboflow COCO 格式”。
2. 填写批次标签。
3. 选择数据集根目录。
4. 填写标签字段名。
5. 选择 Split。
6. 点击“开始导入”。

## 5.6 D. 导入 Roboflow YOLO

**需要填写/选择**

- `数据集根目录（含 data.yaml）`
- `标签字段名`：默认 `ground_truth`
- `选择 Split`：`train` / `valid` / `test`

**操作步骤**

1. 选择“D. Roboflow YOLO 格式”。
2. 填写批次标签。
3. 选择包含 `data.yaml` 的目录。
4. 填写标签字段名。
5. 选择 Split。
6. 点击“开始导入”。

## 5.7 E. 导入 CVAT 1.1 XML

**需要填写**

- `annotations.xml 路径`
- `图片目录（可选，默认为 XML 同级 images/）`
- `标签字段名`：默认 `ground_truth`

**操作步骤**

1. 选择“E. CVAT 1.1 XML 格式”。
2. 填写批次标签。
3. 选择 XML 文件。
4. 如图片不在 XML 同级 `images/`，填写图片目录。
5. 填写标签字段名。
6. 点击“开始导入”。

## 5.8 F. 从备份导入

**用途**  
从本平台完整备份中追加导入图像、标注、Tags。

**需要填写**

- `备份目录路径`：必须包含 `backup_info.json`
- `额外标签（逗号分隔，可选）`

**操作步骤**

1. 选择“F. 从备份导入”。
2. 选择备份目录。
3. 页面显示备份信息后，按需填写额外标签。
4. 点击“📦 开始导入”。

**注意事项**

- 原始 Tags 会保留。
- 同名文件会自动跳过。

---

# 6. 处理清洗

入口：`🧹 处理清洗`

## 6.1 功能速查

| 功能 | 用途 |
| --- | --- |
| 无标注图像 | 查找并删除无标注样本 |
| 损坏/异常图像 | 扫描打不开或尺寸异常的图片 |
| 多边形转四角 | 将多点 polygon 转为四角并排序 |
| 托盘多边形检测 | 检测不合格托盘面 polygon，并打 `bad_polygon` |
| 按标签删除样本 | 删除带指定 Tags 的样本 |
| 重复图像清理 | 精确哈希/近似嵌入去重 |
| 添加字段 | 新建空标注字段 |
| 删除字段 | 永久删除字段或清空字段值 |
| 字段合并 | 将一个标签字段合并到另一个字段 |

## 6.2 删除无标注图像

**需要选择**

- `同时删除磁盘文件`
- `确认删除 ... 个无标注样本`

**操作步骤**

1. 打开“🧹 图像清理”。
2. 查看“无标注样本数”。
3. 按需勾选“同时删除磁盘文件”。
4. 勾选确认。
5. 点击“🗑️ 删除无标注样本”。

## 6.3 扫描并删除异常图像

**需要填写**

- `最小尺寸 (px)`：默认 32。
- `最大尺寸 (px)`：默认 20000。

**操作步骤**

1. 设置最小/最大尺寸。
2. 点击“🔍 扫描异常图像”。
3. 如果发现异常，勾选确认删除。
4. 点击“🗑️ 删除异常样本（含磁盘文件）”。

## 6.4 多边形转四角

**需要选择**

- `多边形字段`
- `操作范围`：整个数据集 / 按标签筛选

**操作步骤**

1. 选择 Polylines 类型字段。
2. 选择操作范围。
3. 打开“🔄 多边形转四角”。
4. 点击“🔄 执行转换”。
5. 查看转换、重排、跳过数量。

**结果规则**

- 多点 polygon：取凸包并转为四角。
- 已经 4 点：只重新排序为 tl / tr / br / bl。
- 无法构成有效四边形：保留原样。

## 6.5 托盘多边形检测

**用途**  
检测不合格托盘面 polygon，并给样本打 `bad_polygon`。

**可检测问题**

- 非四角。
- 两点过近。
- 内角异常。
- 非凸。
- 面积过小。
- 极端细条形。

**需要选择/填写**

| 字段 | 说明 |
| --- | --- |
| 多边形字段 | Polylines 字段 |
| 操作范围 | 整个数据集 / 按标签筛选 |
| 仅检测特定类别 | 可选 |
| 最小点对距离比 | 越大越严格 |
| 最小内角 / 最大内角 | 判断折叠或展平 |
| 最小面积 | 面积过小判异常 |
| 最小紧凑度 | 极端细条判异常 |

**操作步骤**

1. 选择多边形字段和范围。
2. 打开“📐 托盘多边形检测”。
3. 按需选择特定类别。
4. 默认高级参数通常可直接使用。
5. 点击“🔍 扫描不合格托盘多边形”。

**处理异常样本**

- 删除：勾选确认后点击“🗑️ 删除已标记的异常样本”。
- 只清标签：点击“🧹 清除 bad_polygon 标记”。

## 6.6 按标签删除样本

**需要选择**

- `选择标签`
- `同时删除磁盘文件`
- 确认删除

**操作步骤**

1. 选择要删除的 Tags。
2. 查看匹配样本数。
3. 按需勾选“同时删除磁盘文件”。
4. 勾选确认。
5. 点击“🗑️ 删除匹配样本”。

## 6.7 重复图像清理

**需要选择**

| 字段 | 选项 |
| --- | --- |
| 扫描范围 | 整个数据集 / 按标签筛选 |
| 去重模式 | 精确哈希 / 近似嵌入 / 两者兼有 |
| 嵌入模型 | CLIP 或 DINOv2 模型 |
| 余弦距离阈值 | 越小越严格，推荐 `0.01 ~ 0.05` |

**操作步骤**

1. 选择扫描范围。
2. 选择去重模式。
3. 如果使用近似嵌入，选择模型和阈值。
4. 点击“🔍 扫描重复图像”。
5. 查看可删除重复数量。
6. 选择：
   - 勾选“仅打标签不删除”：打 `duplicate` 标签。
   - 不勾选：直接删除重复样本。
7. 如直接删除，按需勾选“同时删除磁盘文件”。
8. 点击“执行去重”。

**已有 duplicate 样本处理**

- 删除：勾选确认后点击“🗑️ 删除已标记的重复样本”。
- 清标签：点击“🧹 清除 duplicate 标记”。

## 6.8 添加字段

**用途**  
添加空标注字段，用于后续预标注或 CVAT 标注。

**需要填写/选择**

- `新字段名称`：例如 `predict`, `ground_truth_v2`
- `字段类型`：Detections / Polylines / Keypoints / Classifications
- `操作范围`：整个数据集 / 按标签筛选

**操作步骤**

1. 打开“🗂️ 字段管理”。
2. 进入“➕ 添加字段”。
3. 输入字段名。
4. 选择字段类型。
5. 选择范围。
6. 点击“➕ 添加字段”。

**注意事项**

- 字段名必须是合法标识符：字母或下划线开头，只含字母、数字、下划线。

## 6.9 删除字段 / 清空字段值

**删除模式**

| 模式 | 作用 |
| --- | --- |
| 从整个数据集永久删除字段 | 删除字段及其所有数据 |
| 仅清空筛选范围内的字段值 | 保留字段，只把部分样本字段值清空 |

**清空字段值步骤**

1. 选择字段。
2. 选择“仅清空筛选范围内的字段值”。
3. 选择范围。
4. 勾选确认。
5. 点击“🧹 清空字段值”。

**永久删除字段步骤**

1. 选择字段。
2. 选择“从整个数据集永久删除字段”。
3. 输入字段名确认。
4. 点击“⚠️ 永久删除字段”。

## 6.10 字段合并

**用途**  
将一个标签字段的标注合并到另一个字段。

**需要选择**

- `源字段`
- `目标字段`
- `合并后删除源字段`

**操作步骤**

1. 进入“🔀 字段合并”。
2. 选择源字段。
3. 选择目标字段。
4. 确认两者类型一致。
5. 按需勾选合并后删除源字段。
6. 点击“🔀 执行合并”。

---

# 7. 自动预标注

入口：`🤖 自动预标注`

## 7.1 模式速查

| 模式 | 用途 | 输出 |
| --- | --- | --- |
| YOLO Pose → 四角多边形 | 用关键点模型检测四角 | Polyline |
| SAM3 辅助标注 | 用文本/范例提示生成标注 | Polyline 或 BBox |
| SAM3 标注标签 | 只判断目标是否存在 | Tags，不生成标注 |

## 7.2 模型来源

| 来源 | 说明 |
| --- | --- |
| 预设模型目录 | 扫描 `~/.dataset_platform/model/` |
| 手动选择路径 | 手动选择模型文件 |

支持模型文件：

- `.pt`
- `.pth`
- `.onnx`
- `.engine`

## 7.3 预标注范围

| 范围 | 含义 |
| --- | --- |
| 仅无标注样本 | 只处理平台判断为无标注的样本 |
| 整个数据集 | 处理全部样本 |
| 按 Tags 筛选 | 只处理带指定 Tags 的样本 |

## 7.4 YOLO Pose → 四角多边形

**用途**  
使用 YOLO Pose 模型检测四个角点，生成闭合四边形 Polyline。

**需要填写/选择**

| 字段 | 说明 |
| --- | --- |
| YOLO Pose 权重 | 模型文件 |
| 输出字段 | 默认 `ground_truth` |
| 置信度阈值 | 默认 `0.25` |
| 仅保留类别 | 可选，逗号分隔 |
| 完全跳过已有标注的样本 | 避免重复处理 |
| 标注 NMS IoU 阈值 | 与已有同类标注重叠超过阈值时抑制 |
| 预标注范围 | 无标注 / 全部 / Tags |

**操作步骤**

1. 选择“🦴 YOLO Pose → 四角多边形”。
2. 选择 YOLO Pose 权重。
3. 设置输出字段。
4. 设置置信度。
5. 按需填写类别过滤。
6. 设置去重选项。
7. 选择预标注范围。
8. 点击“🚀 开始 Pose 预标注”。

## 7.5 SAM3 辅助标注

**用途**  
使用 SAM3 根据文本或已有标注范例自动生成标注。

**输出格式**

- 四角多边形 (Polyline)
- 矩形框 (BBox)

**提示策略**

| 策略 | 说明 |
| --- | --- |
| 联合提示 (文本+范例) | 推荐，同时用文字和已有标注范例 |
| 文本概念 | 只输入目标文字 |
| 图像范例 | 只使用已有标注 bbox 作为视觉范例 |
| 视觉分割 | SAM2 兼容模式，对每个已有标注逐一分割 |

**需要填写/选择**

| 字段 | 说明 |
| --- | --- |
| SAM3 权重 | 模型文件 |
| FP16 半精度加速 | 显存支持时可开启 |
| 输出格式 | Polyline 或 BBox |
| 输出字段 | 写入哪个 Label Field |
| 输出类别名 | 默认 `pallet` |
| 置信度阈值 | 默认 `0.25` |
| 文本提示 | 每行一个提示词 |
| 来源标签 | 只用指定 Tags 样本作为范例来源 |
| 来源字段 | 包含 Polyline/Detection 的字段 |
| 来源类别 | 为空表示使用全部类别 |
| BBox 扩展比例 | 扩大范例 bbox 上下文 |
| 失败标签 | 默认 `sam3_failed` |

**操作步骤**

1. 选择“🎯 SAM3 辅助标注”。
2. 选择 SAM3 权重。
3. 选择输出格式和输出字段。
4. 填写输出类别名。
5. 选择提示策略。
6. 根据策略填写文本提示或选择来源字段/类别/Tags。
7. 设置 BBox 扩展比例。
8. 设置去重选项和失败标签。
9. 选择预标注范围。
10. 点击“🚀 开始 SAM3 预标注”。

**注意事项**

- 文本模式必须有文本提示。
- 图像范例/视觉分割必须有来源字段。
- 联合提示至少需要文本提示或来源字段之一。

## 7.6 SAM3 标注标签

**用途**  
识别图片是否包含目标，只给图片打 Tags，不生成标注。

**需要填写/选择**

| 字段 | 说明 |
| --- | --- |
| SAM3 权重 | 模型文件 |
| 置信度阈值 | 判断目标存在的阈值 |
| 识别成功标签 | 默认 `sam3_found` |
| 未识别标签 | 可选 |
| 预测失败标签 | 默认 `sam3_tag_failed` |
| 提示策略 | 联合提示 / 文本概念 / 图像范例 |
| 识别范围 | 整个数据集 / 按 Tags 筛选 |

**操作步骤**

1. 选择“🏷️ SAM3 标注标签”。
2. 选择 SAM3 权重。
3. 填写成功、未识别、失败标签。
4. 选择提示策略。
5. 填写文本提示或选择范例来源。
6. 选择识别范围。
7. 点击“🚀 开始 SAM3 标签识别”。

## 7.7 清除标签

**用途**  
从样本中批量清除某个 Tag。

**操作步骤**

1. 展开“🗑️ 清除标签”。
2. 选择要清除的标签。
3. 点击“清除标签”。

---

# 8. CVAT 同步

入口：`🔄 CVAT 同步`

## 8.1 推送到 CVAT

**用途**  
把当前数据集或子集上传到 CVAT 进行人工标注。

**需要填写**

| 字段 | 说明 |
| --- | --- |
| anno_key | 标注运行唯一键，例如 `round1_all` |
| CVAT 项目名 | 可选，留空使用数据集名称 |
| 标签字段 | 推送已有标注作为预标注，或输入新字段从零标注 |
| 推送范围 | 整个数据集 / 仅无标注 / 仅有标注 |
| 批次标签筛选 | 可选，只推送指定 Tags 样本 |
| 每个 Job 图片数 | 默认 200 |
| 图像质量 | 默认 100 |
| 启用废弃标记 | 允许标注员标记废弃图片 |

**字段来源选择**

| 模式 | 用途 |
| --- | --- |
| 选择已有字段 | 把已有标注作为预标注推送 |
| 输入新字段名 | 在 CVAT 中从零开始标注 |

**操作步骤**

1. 打开“📤 推送到 CVAT”。
2. 输入 anno_key。
3. 按需填写 CVAT 项目名。
4. 选择已有字段，或输入新字段名并选择标注类型。
5. 如需多边形遮挡属性，勾选四角遮挡属性。
6. 按需按字段筛选类别。
7. 选择推送范围。
8. 按需选择批次 Tags。
9. 设置每个 Job 图片数和图像质量。
10. 按需勾选“启用废弃标记”。
11. 点击“📤 推送到 CVAT”。

**注意事项**

- anno_key 只能包含字母、数字、下划线、短横线。
- 每次推送必须使用不同 anno_key。
- 多字段推送会生成类似 `anno_key_字段名` 的派生 key。

## 8.2 标注员在 CVAT 标记废弃图片

如果推送时启用了废弃标记，标注员操作如下：

1. 打开 CVAT 对应任务。
2. 进入标注界面。
3. 切换到 `Tag` 标注模式。
4. 选择 `discard`。
5. 遇到废弃图片时点击画面添加 `discard`。
6. 切回 `Shape` 模式继续标注。

## 8.3 从 CVAT 拉取

**用途**  
把 CVAT 标注结果同步回当前数据集。

**需要选择**

- `选择标注运行`：推送时的 anno_key。

**可选项**

| 选项 | 说明 |
| --- | --- |
| 拉取后删除 CVAT 端任务 | 拉取完成后清理 CVAT 任务 |
| 跳过自动 Job 状态标签 | 大数据集可先跳过，后续在 Data Hub 刷新 |

**操作步骤**

1. 打开“📥 从 CVAT 拉取”。
2. 选择标注运行。
3. 按需勾选清理 CVAT 端任务。
4. 按需勾选跳过自动状态标签。
5. 点击“📥 拉取标注”。

## 8.4 管理标注运行

**用途**  
查看、删除本地 annotation run 记录，可选同时删除 CVAT 端任务。

**操作步骤**

1. 打开“📋 管理标注运行”。
2. 选择要删除的运行。
3. 按需勾选“同时清理 CVAT 端”。
4. 输入 anno_key 确认。
5. 点击“🗑️ 确认删除标注运行”。

## 8.5 任务状态与审核

**用途**  
查看 CVAT Job 的状态、阶段、标注员和完成进度。

**操作步骤**

1. 打开“🔍 任务状态与审核”。
2. 选择一个或多个标注运行。
3. 点击“🔍 查询 Job 状态”。
4. 查看 Job 表格、完成进度、State/Stage 统计。
5. 点击 Task 链接可直接打开 CVAT 任务。

## 8.6 废弃图片管理

**用途**  
处理 CVAT 中被标记为 `discard` 的图片。

**处理方式**

| 方式 | 说明 |
| --- | --- |
| 仅打标签 | 给废弃样本添加 `discarded` 等标签 |
| 从数据集移除（保留文件） | 删除样本记录，不删图片文件 |
| 从数据集移除并删除文件 | 删除样本记录和图片文件 |

**操作步骤**

1. 在“任务状态与审核”中找到“废弃图片管理”。
2. 如有废弃样本，可点击“👁️ 在 FiftyOne App 中查看废弃样本”。
3. 选择处理方式。
4. 如果仅打标签，填写标签名。
5. 如果移除样本，输入数量确认。
6. 点击“✅ 执行处理”。

---

# 9. 数据导出

入口：`📤 数据导出`

## 9.1 导出格式速查

| 格式 | 用途 |
| --- | --- |
| YOLO Detect (纯框) | 导出检测框训练数据 |
| YOLO Pose (关键点) | 导出框 + 关键点 |
| YOLO 混合训练（pose） | 四边形类转关键点，检测类写 bbox + 全 0 关键点 |
| YOLO Pose (四边形转关键点) | 4 点 Polyline 转 tl/tr/br/bl 关键点 |
| YOLO OBB (旋转框) | 导出旋转框四点 |
| 纯图片 | 只导出图片，不导出标签和 data.yaml |

## 9.2 通用导出设置

**需要填写/选择**

- `导出格式`
- `输出目录`
- `按 Tag 筛选样本（留空导出全部）`

**操作步骤**

1. 选择导出格式。
2. 选择输出目录。
3. 按需选择 Tags。
4. 留空 Tags 表示导出全部样本。

## 9.3 数据划分

YOLO 格式需要设置划分方式。

| 模式 | 字段 |
| --- | --- |
| 按比例自动划分 | Train % / Valid % / Test %，总和必须 100 |
| 指定单个 Split | train / valid / test |

## 9.4 类别顺序锁定

**用途**  
增量训练时保持旧类别 class id 不变，新类别追加到末尾。

**操作步骤**

1. 展开“🔒 类别顺序锁定（增量训练推荐）”。
2. 选择上一版 `data.yaml`。
3. 确认平台读取到旧类别顺序。
4. 正常导出。

## 9.5 background 负样本

适用格式：

- YOLO 混合训练（pose）
- YOLO Pose (四边形转关键点)

**用途**  
将带 `background` Tag 的图片全部加入 `train/images`，并生成空标签文件。

**操作步骤**

1. 确保背景图有 `background` Tag。
2. 选择支持该功能的导出格式。
3. 勾选“将 background 标签图片全部加入 train 作为负样本”。

## 9.6 各格式额外字段

| 格式 | 额外需要填写 |
| --- | --- |
| YOLO Detect | 标签字段、类别筛选 |
| YOLO Pose | 标签字段、关键点字段名、类别筛选 |
| YOLO 混合训练（pose） | 四边形字段、检测框字段、只检测类别、边界阈值、BBox 外扩边距 |
| YOLO Pose (四边形转关键点) | 四边形字段、边界阈值、BBox 外扩边距 |
| YOLO OBB | 标签字段、OBB 字段名（可选） |
| 纯图片 | 无额外标签配置 |

## 9.7 开始导出

**操作步骤**

1. 配置格式、输出目录、Tags、划分、字段、类别。
2. 点击“📦 开始导出”。
3. 查看导出结果 JSON。
4. 如果生成 `data.yaml`，页面会显示路径。
5. 可直接前往训练管理页面使用该 `data.yaml`。

---

# 10. 训练管理

入口：`🏋️ 训练管理`

## 10.1 页签速查

| 页签 | 用途 |
| --- | --- |
| 训练配置与启动 | 配模型、数据、超参数并启动训练 |
| 训练进度与历史 | 看实时进度、曲线、历史、best.pt |
| 模型格式转换 | 导出 ONNX / TensorRT / OpenVINO 等 |
| 通用预测 | 用模型对图片、视频、目录、URL 推理 |
| 训练后回灌 | 用 best.pt 预标注并评估，为难例挖掘准备 |

## 10.2 训练配置：模型

**需要选择/填写**

| 字段 | 说明 |
| --- | --- |
| 基础模型来源 | 基础模型目录 / 自定义路径 / 输入模型名称 |
| 基础模型目录 | `~/.dataset_platform/base_model/` |
| 任务类型 | detect / pose / obb / classify / segment |
| 训练输出目录 | 默认路径 / 自定义路径 |
| project | 默认 `/home/cotek/ws_cotek/ultralytics/runs/{task}` |

## 10.3 训练配置：数据

**需要选择/填写**

| 字段 | 说明 |
| --- | --- |
| data.yaml 来源 | 默认路径 / 自定义路径 |
| data.yaml 路径 | 最近导出路径或 `/home/cotek/datasets/temp/data.yaml` |
| 运行名称 | 可留空自动命名 |

## 10.4 增量训练 / 旧类回放

**用途**  
新增类别时回放旧类别，降低遗忘风险。

**需要填写**

- `启用增量训练回放`
- `新增类别`
- `每个旧类回放图片上限`
- `抽样种子`
- `新类自动重采样`

**注意事项**

- 启用后必须选择新增类别。
- 推荐导出时先使用“类别顺序锁定”。

## 10.5 训练超参数

| 字段 | 默认/说明 |
| --- | --- |
| Epochs | 默认 150 |
| Image Size | 默认 640 |
| Batch Size | 默认 16 |
| Device | `0` / `cpu` / `0,1` |

## 10.6 增强与优化参数

| 分组 | 字段 |
| --- | --- |
| 色彩空间增强 | hsv_h / hsv_s / hsv_v |
| 几何变换 | degrees / translate / scale / shear / perspective / fliplr / flipud |
| 组合增强 | mosaic / close_mosaic / mixup / cutmix / multi_scale |
| 类别平衡 | 少数类重采样、少数类名称、参考多数类、目标比例、最大重复倍数 |
| 损失权重 | cls / box / dfl |
| 优化器与学习率 | lr0 / lrf / weight_decay / optimizer / cos_lr / patience / momentum / warmup |

## 10.7 开始训练

**操作步骤**

1. 确认 `data.yaml` 存在。
2. 确认模型路径或模型名称已填写。
3. 如启用增量训练，确认已选择新增类别。
4. 点击“🚀 开始训练”。
5. 去“训练进度与历史”查看进度。

## 10.8 训练进度与历史

**训练中显示**

- 当前轮数。
- 已用时间。
- 预计剩余。
- 进度条。
- 当前指标。
- Loss 曲线。
- 精度曲线。

**训练完成显示**

- 开始时间。
- 训练用时。
- Epochs。
- mAP、Precision、Recall、Loss。
- 完整训练记录 JSON。
- 历史训练表格。

## 10.9 最优权重管理

**用途**  
管理当前数据集的 `last_best_pt`。

**操作步骤**

1. 查看“当前最优权重”。
2. 点击“📋 设为预标注模型”，可用于自动预标注。
3. 在历史权重中选择某个 best.pt。
4. 点击“设为当前最优权重”。

## 10.10 模型格式转换

**权重来源**

- 最新 best.pt
- 历史权重
- 自定义路径
- 输入模型名称

**转换参数**

| 字段 | 说明 |
| --- | --- |
| 目标格式 | ONNX / Engine / OpenVINO 等 |
| 任务类型 | 自动检测或手动选择 |
| Image Size | `640` 或 `640 480` |
| Device | GPU 或 CPU |
| Batch Size | 导出 batch |
| ONNX Opset | 0 表示自动 |
| TensorRT Workspace GB | 0 表示自动 |
| INT8 校准数据比例 | INT8 校准使用 |
| 高级选项 | half / int8 / dynamic / simplify / nms / keras / optimize |

**操作步骤**

1. 打开“🔁 模型格式转换”。
2. 选择权重来源。
3. 设置目标格式和参数。
4. 点击“🔁 开始转换”。
5. 查看输出路径和转换历史。

## 10.11 通用预测

**输入源类型**

- 默认文件夹。
- 自定义文件夹。
- 图片/视频文件。
- URL / 流地址。

**推理参数**

- 任务类型。
- 置信度。
- NMS IoU。
- 图片尺寸。
- Device。
- 最大检测数。
- FP16 半精度。

**输出控制**

- 保存标注图/视频。
- 保存 txt 标签。
- txt 中包含置信度。
- 保存裁剪目标。
- 输出目录。
- 子目录名。
- 允许覆盖已有目录。

**操作步骤**

1. 打开“🔮 通用预测”。
2. 选择模型权重。
3. 选择输入源并填写路径/URL。
4. 设置推理参数。
5. 按需设置输出控制和显示控制。
6. 点击“🔮 开始预测”。

## 10.12 训练后回灌

**用途**  
用当前 best.pt 对数据集预标注并评估，为难例挖掘准备。

**需要填写**

| 字段 | 默认 |
| --- | --- |
| 预测结果字段 | `predictions` |
| 真值字段 | `ground_truth` |
| 任务类型 | detect / pose / obb |
| 置信度阈值 | 默认 0.25 |
| 评估键 | `eval` |

**操作步骤**

1. 确认已有当前最优权重。
2. 打开“🔄 训练后回灌”。
3. 填写预测字段和真值字段。
4. 选择任务类型。
5. 点击“🔄 执行回灌（预标注 + 评估）”。
6. 回灌后去“高级功能 → 难例挖掘”。

---

# 11. 质量检查

入口：`🔬 质量检查`

## 11.1 页签速查

| 页签 | 用途 |
| --- | --- |
| 类别平衡 | 看类别数量是否均衡，并可过采样 |
| 面积分析 | 找异常小/大的 bbox |
| 空标注检测 | 找字段存在但内容为空的样本 |
| 标注一致性 | 对比多个 anno_key 的类别分布 |

## 11.2 类别平衡

**操作步骤**

1. 选择标签字段。
2. 打开“📊 类别平衡”。
3. 查看柱状图、表格、最大/最小类别比。
4. 查看平台给出的补采、过采样、欠采样建议。

## 11.3 过采样工具

**用途**  
复制少数类样本，并可随机增强。

**需要填写/选择**

- 目标数量策略：对齐到最大类 / 自定义目标数量。
- 每个类别目标实例数。
- 需要过采样的类别。
- 是否随机增强。
- 随机旋转角度范围。
- 是否只使用纯类别源图。
- 源图片允许包含的类别。

**操作步骤**

1. 在“类别平衡”下方找到“过采样工具”。
2. 选择目标数量策略。
3. 选择过采样类别。
4. 查看计划表。
5. 按需开启随机增强和源样本限制。
6. 点击“🚀 执行过采样”。

**结果**

- 新增样本会带 `oversampled`。
- 新增样本会带 `os_类别名`。

## 11.4 面积分析

**需要填写**

- `最小面积阈值 (%图像面积)`
- `最大面积阈值 (%图像面积)`

**操作步骤**

1. 打开“📐 面积分析”。
2. 设置面积阈值。
3. 点击“🔍 分析面积分布”。
4. 查看异常小/异常大样本数。
5. 点击按钮在 FiftyOne 中查看异常样本。

## 11.5 空标注检测

**操作步骤**

1. 打开“🔲 空标注检测”。
2. 点击“🔍 检测空标注”。
3. 如有结果，点击“👁️ 在 FiftyOne 中查看”。

## 11.6 标注一致性

**用途**  
对比不同 CVAT 标注运行之间的类别分布。

**操作步骤**

1. 打开“🔍 标注一致性”。
2. 选择至少 2 个标注运行。
3. 点击“🔍 分析一致性”。
4. 查看不同 anno_key 的类别数量对比。

---

# 12. 高级功能

入口：`🧠 高级功能`

## 12.1 难例挖掘

**用途**  
对比预测字段和真值字段，找模型表现差的样本。

**需要填写**

| 字段 | 默认 |
| --- | --- |
| 预测字段 | `predictions` |
| 真值字段 | `ground_truth` |
| IoU 阈值 | 0.5 |
| 最多显示难例数 | 50 |

**操作步骤**

1. 打开“🎯 难例挖掘”。
2. 填写预测字段和真值字段。
3. 设置 IoU 阈值。
4. 点击“📊 评估模型表现”。
5. 设置最多显示难例数。
6. 点击“🎯 挖掘难例”。
7. FiftyOne 中会展示难例视图。

## 12.2 难例推送 CVAT 重标注

**需要填写**

- 是否为难例打 `hard_sample` 标签。
- `CVAT 标注键`，默认 `hard_relabel`。
- `标注字段`。

**操作步骤**

1. 先执行“挖掘难例”。
2. 勾选是否添加 `hard_sample`。
3. 填写 CVAT 标注键。
4. 填写标注字段。
5. 点击“📤 推送难例到 CVAT”。

## 12.3 FiftyOne Brain

| 功能 | 用途 | 输出字段 |
| --- | --- | --- |
| Uniqueness | 找相似/独特样本 | `uniqueness` |
| Hardness | 找模型最不确定样本 | `hardness` |
| Representativeness | 找代表性低的边缘样本 | `representativeness` |

**Uniqueness 操作**

1. 点击“🔍 计算 Uniqueness”。
2. 查看最不唯一样本或最独特样本。
3. 点击“📊 查看统计”看均值、最小值、最大值。

**Hardness 操作**

1. 填写预测字段。
2. 点击“🎯 计算 Hardness”。
3. 查看最难样本。

**Representativeness 操作**

1. 点击“📊 计算 Representativeness”。
2. 查看代表性最低样本。

## 12.4 数据集完整备份

**用途**  
复制图像文件并导出元数据，创建可迁移完整备份。

**创建备份需要填写**

- `备份根目录`
- `备份备注`
- `备份子目录名（可选）`

**创建步骤**

1. 打开“💾 数据集备份”。
2. 选择备份根目录。
3. 填写备注。
4. 按需填写备份子目录名。
5. 点击“💾 创建完整备份”。

**恢复步骤**

1. 选择已有备份。
2. 填写“恢复为数据集名称”。
3. 点击“♻️ 从备份恢复”。
4. 输入数据集名称确认。
5. 点击“✅ 确认恢复”。

**删除备份步骤**

1. 选择已有备份。
2. 勾选“⚠️ 确认删除此备份”。
3. 点击“🗑️ 删除此备份”。

---

# 13. 推荐工作流

## 13.1 从零开始做一批数据

1. 侧边栏新建数据集。
2. 数据导入：导入图片或已有标注，填写批次 Tags。
3. Data Hub：查看样本数、Tags、类别统计。
4. 自动预标注：用 YOLO Pose 或 SAM3 生成初始标注。
5. CVAT 同步：推送到 CVAT。
6. CVAT：人工检查、修正、补标。
7. CVAT 同步：拉取标注。
8. 质量检查：看类别平衡、面积异常、空标注、一致性。
9. 处理清洗：删除异常、重复、无效样本。
10. 数据导出：导出 YOLO 训练数据。
11. 训练管理：启动训练。
12. 训练完成：设置 best.pt 为当前最优权重。
13. 训练后回灌：生成 predictions 并评估。
14. 高级功能：挖掘难例。
15. 推送难例到 CVAT 重标注，进入下一轮迭代。

## 13.2 多批次数据管理

1. 每批导入都填写 Tags，例如 `batch_01`、`batch_02`。
2. 人工标注尽量统一写入 `ground_truth`。
3. 推送 CVAT 时按 Tags 筛选批次。
4. 每次推送使用独立 anno_key，例如 `round1_batch01`。
5. 拉取后在 Data Hub 刷新 Job 状态标签。
6. 导出训练集时按 Tags 选择参与训练的样本。

## 13.3 新增类别增量训练

1. 导入新类别数据并打批次 Tags。
2. 完成新类别标注。
3. 数据导出时启用“类别顺序锁定”，选择上一版 `data.yaml`。
4. 确认旧类别顺序不变，新类别追加。
5. 训练管理中启用“增量训练 / 旧类回放”。
6. 选择新增类别。
7. 设置旧类回放图片上限。
8. 启动训练。

---

# 14. 高风险操作提醒

执行前建议先创建完整备份。

| 高风险操作 | 风险 |
| --- | --- |
| 删除数据集 | 删除当前 Dataset |
| 删除无标注样本并勾选删除磁盘文件 | 删除真实图片文件 |
| 删除异常样本 | 删除样本和图片 |
| 按标签删除样本 | 可能批量删错 |
| 删除重复样本 | 可能误删近似但不重复图片 |
| 永久删除字段 | 字段和标注全部丢失 |
| 批量删除类别标注 | 标注实例不可恢复 |
| 拉取后删除 CVAT 端任务 | CVAT 任务永久删除 |
| 删除标注运行并清理 CVAT | 本地记录和 CVAT 任务都删除 |
| 移除废弃图片并删除文件 | 删除样本和图片文件 |
| 从备份恢复覆盖数据集 | 同名数据集被覆盖 |
| 删除备份 | 备份目录被删除 |

**建议**

1. 先打 Tags，再筛选查看，最后删除。
2. 删除前先在 FiftyOne 中查看样本。
3. 大批量操作前先完整备份。

---

# 15. 常见问题

## 15.1 页面提示“请先选择数据集”

**处理**

1. 在侧边栏选择已有数据集。
2. 如果没有数据集，先新建数据集。

## 15.2 CVAT 推送失败

**检查**

1. CVAT URL、用户名、密码是否正确。
2. Organization 是否需要填写。
3. 是否测试连接成功。
4. anno_key 是否已经存在。
5. 推送范围内是否有样本。
6. 标签字段、标注类型、类别是否正常。

## 15.3 拉取后没有 CVAT 状态 Tags

**可能原因**

- 拉取时勾选了“跳过自动 Job 状态标签”。
- 缺少 frame_id 映射。

**处理**

1. 打开 Data Hub。
2. 进入“标签筛选与查看”。
3. 在“CVAT Job 状态标签刷新”中选择 anno_key。
4. 点击刷新。

## 15.4 训练页面找不到模型

**处理**

1. 将模型放到 `~/.dataset_platform/base_model/`。
2. 或选择“自定义路径”手动选择。
3. 或选择“输入模型名称”，例如 `yolo11n.pt`。

## 15.5 自动预标注找不到模型

**处理**

1. 将模型放到 `~/.dataset_platform/model/`。
2. 或选择“手动选择路径”。
3. 确认扩展名是 `.pt`、`.pth`、`.onnx` 或 `.engine`。

## 15.6 导出后训练页面没有自动填入 data.yaml

**处理**

1. 确认导出格式不是“纯图片”。
2. 确认导出成功并生成 `data.yaml`。
3. 在训练页面选择“自定义路径”，手动选择导出的 `data.yaml`。

## 15.7 想撤销过采样

**处理**

1. 过采样新增样本会带 `oversampled` 标签。
2. 在 Data Hub 按 `oversampled` 筛选确认。
3. 在处理清洗页面按标签删除样本。

